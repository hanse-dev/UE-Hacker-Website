# ⭐⭐☆☆☆ Mission 1: The Guardian Seal

The first gate is locked with three guardian seals. Each opens only when you call the right guardian (the module) and prove its power.

**Bonus (optional, not checked):** Add a timestamp with from datetime import datetime for when you finished.
