"""### ⭐⭐☆☆☆ Mission 1: The Data Archivist

In the data archives of Nebula-7 you create your first dictionary for ship data.

**Step 1 – Create a ship dictionary:**
Create a dictionary `ship` with keys e.g. `name`, `type`, `crew`, `speed` and suitable values, and print the dictionary using `print()`

**Step 2 – Change and extend values:**
Change the value of `crew` (e.g. to a new number) and add a new key `captain` with a value

**Step 3 – Planet list of dictionaries:**
Create a list with 3 elements; each element is a dictionary with e.g. `name` and `distance` (or similar keys), and print the list and the values of one planet (e.g. `planets[0][\"name\"]`)

**Step 4 – Access and output:**
Access values with `ship[\"name\"]` and `ship.get(\"type\")` and print them, and print a short summary (e.g. \"Ship X, Type Y, Crew Z\")

**Bonus:** Create a nested dictionary (e.g. ship with a sub-dictionary \"systems\")."""
