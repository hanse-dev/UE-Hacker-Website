# ⭐⭐⭐⭐☆ Mission 3: The Oracle of the Four Elements

The final trial: consult the oracle about the elements of a seeker. Make sure to convert every answer into the right type. Type exactly the values given in the task into the input box.

**Bonus (optional, not checked):** Use `type()` to print the element of every answer.
