# ⭐⭐⭐☆☆ Mission 2: The Elemental Fusion

In the fusion chamber you test which elements can be fused directly – and which need a conversion.

**Bonus (optional, not checked):** Try step 4 *without* `str()` and note the error message as a comment.
