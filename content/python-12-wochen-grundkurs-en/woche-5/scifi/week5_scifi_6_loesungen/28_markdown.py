"""### System Log 7: Docstrings and good names – Task 2

The function is called cs(l, w) – a bad name. Rename it to calculate_cargo_space(length, width) (also the parameters in the body) so the call below works and prints exactly Cargo space: 21"""