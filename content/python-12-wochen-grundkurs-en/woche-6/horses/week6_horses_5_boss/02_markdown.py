"""### ⭐⭐⭐⭐☆ Boss Quest 1: The Training Generator

Build a system that automatically generates, filters and sorts training sessions.

**Step 1 – Generate training:**
Write `generate_training(horse_type, difficulty)`, which returns a list of entries (e.g. names or IDs depending on type/difficulty) and return the list with `return`

**Step 2 – Calculate statistics:**
Create `calculate_training_stats(training_list)`, which returns the length and optionally the average and print the values as a dictionary or printed output

**Step 3 – Create multiple training sessions:**
Create a list `trainings` and add at least 10 training sessions using a loop (e.g. with `generate_training(\"Sport Horse\", i)`) and use `append()` for each entry

**Step 4 – Filter and sort training sessions:**
Write `filter_trainings(training_list, search_term)`, which returns only entries that contain the search term and sort the list with `sort()` or `sorted()` and print the result

**Step 5 – Assemble the generator:**
Call all functions in the main section and print a short summary (count, statistics, filtered count)

**Bonus:** Add random attributes (e.g. duration, intensity) and validate the data."""
