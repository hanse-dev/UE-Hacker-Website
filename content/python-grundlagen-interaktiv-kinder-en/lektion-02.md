# Variables – Your Memory 🧠

A variable is like a treasure chest with a name. You put something inside and can take it out again whenever you like!

```python
animal = "Dog"
print(animal)  # prints "Dog"
```

You can change the contents at any time:

```python
animal = "Cat"
print(animal)  # now prints "Cat"
```
