"""### ⭐⭐⭐⭐☆ Mission 3: Die CSV-Datenbank

Erstelle ein komplettes CSV-Verwaltungssystem!

**Schritt 1 – CSV-Export:**
Schreibe eine Liste von Helden-Dictionaries (`name`, `klasse`, `level`) mit `csv.DictWriter` in `helden.csv` (mit `newline=\"\"`) und gib eine Bestätigung aus

**Schritt 2 – CSV-Import:**
Lies die Datei mit `csv.DictReader` ein, sammle die Zeilen in einer Liste und gib die Anzahl sowie die erste Zeile aus

**Schritt 3 – Suchfunktion:**
Schreibe eine Funktion, die nur die Zeilen mit einer bestimmten Klasse zurückgibt, und rufe sie auf und gib das Ergebnis aus

**Schritt 4 – Kurzer Bericht:**
Berechne eine kleine Statistik (z.B. Durchschnittslevel, Anzahl pro Klasse) aus den gelesenen Daten und gib sie aus

**Beispiel-Code:** `csv.DictWriter`/`csv.DictReader`, `fieldnames=[\"name\", \"klasse\", \"level\"]`.

**Bonus:** Prüfe, ob alle erwarteten Spalten in der CSV-Datei vorhanden sind."""
