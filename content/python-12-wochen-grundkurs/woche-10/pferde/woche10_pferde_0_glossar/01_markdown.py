"""# 📖 Glossar – 🐴 Woche 10 – OOP Grundlagen: Die magischen Blaupausen!
> Dieses Notebook kannst du die ganze Woche offen lassen.

| Begriff | Bedeutung | Beispiel |
|---------|-----------|----------|
| `class` | Eine eigene Klasse (Bauplan für Objekte) definieren | `class Held:` |
| **Objekt** | Eine konkrete Instanz einer Klasse | `held = Held(\"Aria\")` |
| **Instanz** | Ein einzelnes erstelltes Objekt einer Klasse | `held = Held()` |
| `__init__` | Konstruktor *(= spezielle Methode, die beim Erstellen eines Objekts automatisch startet)* – wird beim Erstellen eines Objekts aufgerufen | `def __init__(self, name):` |
| `self` | Verweis auf das eigene Objekt innerhalb der Klasse | `self.name = name` |
| **Attribut** | Eigenschaft / Variable eines Objekts | `held.name` |
| **Methode** | Funktion eines Objekts (verwendet `self`) | `held.angreifen()` |
| **Konstruktor** | `__init__` – Methode die beim Erstellen eines Objekts automatisch startet | `def __init__(self, name):` |
| **Instanz** | Ein einzelnes erstelltes Objekt einer Klasse | `held = Held(\"Aria\")` |"""
