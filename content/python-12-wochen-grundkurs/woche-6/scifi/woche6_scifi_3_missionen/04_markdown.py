"""### ⭐⭐⭐☆☆ Mission 2: Der System-Manager

Du verwaltest die Schiffssysteme der Raumstation mit Listen und kleinen Hilfsfunktionen.

**Schritt 1 – Systemliste anlegen:**
Erstelle eine Liste `systeme` mit verschiedenen Schiffssystemen (z.B. \"Antrieb\", \"Schild\", \"Waffen\") und gib die Liste und die Anzahl der Systeme mit `len()` aus

**Schritt 2 – Systeme hinzufügen:**
Schreibe eine Funktion `system_hinzufuegen(liste, name)` oder nutze direkt `append()` und `insert()`, um Systeme hinzuzufügen und füge mindestens ein System am Ende und eines an einer bestimmten Position (z.B. Index 0) ein

**Schritt 3 – Systeme entfernen und suchen:**
Entferne ein System mit `remove()` und eines mit `pop()` (z.B. letztes Element) und finde mit `index()` die Position eines bestimmten Systems

**Schritt 4 – Statistik ausgeben:**
Gib Anzahl der Systeme, die aktuelle Liste und optional eine kurze Statuszeile aus

**Bonus:** Speichere zu jedem System eine Priorität (z.B. in einer zweiten Liste oder als Zahl) und gib eine sortierte Übersicht nach Priorität aus."""
