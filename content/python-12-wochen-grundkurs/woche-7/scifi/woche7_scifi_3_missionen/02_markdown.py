"""### ⭐⭐☆☆☆ Mission 1: Der Modul-Importer

Das Kontrollsystem der Nebula-7 lässt dich erst ins Modul-Zentral, wenn dein Shuttle vier Systemchecks nacheinander besteht – jeder nutzt ein anderes importiertes Modul:

**Schritt 1 – Module importieren:**
Lade die Module `math`, `random` und `time` mit passenden Aliasen (z.B. `import math as m`) und gib jeweils eine Konstante oder Funktion aus jedem Modul aus (z.B. `math.pi`, `random.randint(1, 10)`)

**Schritt 2 – Wartezeit-Simulation:**
Erstelle eine kleine Funktion oder ein Skript, das eine zufällige Sekundenzahl (1–3) mit `random` erzeugt und `time.sleep()` nutzt und gib vor und nach der Wartezeit eine kurze Meldung aus (z.B. \"Andockmanöver läuft...\")

**Schritt 3 – Flugbahn-Berechnung:**
Nutze `math.sin` und `math.pi`, um für einen Winkel (z.B. 90° in Bogenmaß) eine Höhe oder einen Wert zu berechnen und gib das Ergebnis formatiert aus – das Kontrollsystem prüft damit deinen Kurs

**Schritt 4 – Crew-Generator:**
Erstelle eine Namensliste mit 3–5 Crew-Namen und wähle mit `random.choice()` zufällig aus, wer den Landungstrupp anführt, und gib den ausgewählten Namen aus

**Bonus:** Füge eine weitere Funktion mit dem `datetime`-Modul hinzu (z.B. aktuelle Uhrzeit anzeigen)."""
