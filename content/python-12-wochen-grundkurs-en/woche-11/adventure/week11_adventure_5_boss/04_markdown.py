"""### ⭐⭐⭐⭐⭐ Boss Quest 2: The Mage Family

Every mage has a basic education – but each school has its own strength!

**Step 1 – Base class:**
Write a class `Mage` with name and mana. Add a method `cast_spell()`.

**Step 2 – Schools:**
Derive 3 child classes (e.g. FireMage, IceMage, LightningMage). Each overrides `cast_spell()` with its own spell style.

**Step 3 – Duel:**
Create one representative of each school. Let all three cast their spell and show a nice introduction with `__str__`.

**Bonus:** Add `__eq__` that compares two mages by mana."""
