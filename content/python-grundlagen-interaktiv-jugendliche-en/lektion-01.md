# Your First Program

`print()` outputs text or values to the console. It is the most fundamental Python function – you'll find it in every program, whether for debugging or as actual output.

```python
print("Hello World")
print(42)
print(3.14)
```

With `#` you write comments – Python ignores them. Useful for explanations in the code.
