# ⭐⭐⭐☆☆ Mission 2: The Terminal Access

The terminal only grants access when the correct password is entered!

- Admin password: `"nebula42"` → prints `Full access granted`
- Guest password: `"visitor7"` → prints `Read access granted`
- Anything else → prints `Wrong password!`

Simulate the input with a variable `entered` and check it with `if`, `elif` and `else`.

**Bonus (optional, not checked):** Give different hints for wrong passwords!
