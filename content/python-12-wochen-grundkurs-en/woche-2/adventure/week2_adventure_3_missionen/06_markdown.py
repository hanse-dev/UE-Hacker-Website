"""### ⭐⭐⭐⭐☆ Mission 3: The Oracle of the Four Elements

The final trial: consult the oracle about the four elements of a seeker and deliver a prophecy.

**Step 1 – Consult 🔥 Fire:**
Use `input()` to ask for the seeker's name (text)

**Step 2 – Consult 🪨 Earth:**
Use `input()` to ask for their age and convert the answer with `int()`

**Step 3 – Consult 💧 Water:**
Use `input()` to ask for a magic power value (e.g. \"7.5\") and convert the answer with `float()`

**Step 4 – Determine 💨 Air:**
Calculate a truth value for whether the magic power is above `5.0` (comparison `>`), and print a prophecy that summarises all four answers

**Bonus:** Also print the element (type) of each of the four answers using `type()`."""
