"""### ⭐⭐⭐☆☆ Mission 2: The Coordinate Rider

You use tuples for 2D coordinates (riding paths) and combine them with dictionaries.

**Step 1 – Coordinates as tuples:**
Create at least two 2D coordinates as tuples (e.g. `pos1 = (100, 200)` for waypoints) and print the tuples and their length with `len(pos1)`

**Step 2 – Tuple unpacking:**
Use unpacking: `x, y = pos1` and print x, y; create a second coordinate and unpack it too

**Step 3 – Positions dictionary:**
Create a dictionary `positions` where keys are names (strings, e.g. `\"Stable\"`) and values are coordinate tuples (e.g. `{\"Stable\": (0, 0), \"Arena\": (50, 30)}`); print the dictionary and access one position (e.g. `positions[\"Stable\"]`)

**Step 4 – Distance (simplified):**
Write a simple function `distance(coord1, coord2)` that calculates the Euclidean distance for 2D: √((x1-x2)² + (y1-y2)²); call it for two of your coordinates and print the result

**Bonus:** Add 3D coordinates (x, y, z) as tuples and extend the distance calculation."""
