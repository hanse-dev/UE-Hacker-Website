"""### ⭐⭐☆☆☆ Mission 1: The Stable Archivist

At Sunny Valley Riding Ranch you create your first stable card (dictionary) for horse data.

**Step 1 – Create a horse stable card:**
Create a dictionary `horse` with keys such as `name`, `breed`, `age`, `height` and suitable values, and print the dictionary with `print()`

**Step 2 – Change and extend values:**
Change the value of `age` (e.g. to a new number) and add a new key `owner` with a value

**Step 3 – List of horse dictionaries:**
Create a list with 3 elements; each element is a dictionary with e.g. `name`, `breed`, `age` and print the list and the values of one horse (e.g. `horses[0][\"name\"]`)

**Step 4 – Access and output:**
Access values with `horse[\"name\"]` and `horse.get(\"breed\")` and print them, then print a short clear summary (e.g. \"Horse X, Breed Y, Age Z\")

**Bonus:** Create a nested dictionary (e.g. a horse with a sub-dictionary `\"health\"`)."""
