"""### ⭐⭐⭐⭐☆ Boss-Quest 1: Der Trainings-Generator

Baue ein System, das automatisch Trainings erzeugt, filtert und sortiert.

**Schritt 1 – Training erzeugen:**
Schreibe `generiere_training(pferdetyp, schwierigkeit)`, die eine Liste mit Einträgen zurückgibt (z.B. Namen oder IDs je nach Typ/Schwierigkeit) und gib die Liste mit `return` zurück

**Schritt 2 – Statistik berechnen:**
Erstelle `berechne_trainings_statistik(trainingsliste)`, die Länge und optional Durchschnitt zurückgibt und gib die Werte als Dictionary oder ausgedruckt zurück

**Schritt 3 – Mehrere Trainings anlegen:**
Erstelle eine Liste `trainings` und füge mit einer Schleife mindestens 10 Trainings hinzu (z.B. mit `generiere_training(\"Sportpferd\", i)`) und nutze `append()` für jeden Eintrag

**Schritt 4 – Trainings filtern und sortieren:**
Schreibe `filtere_trainings(trainingsliste, suchbegriff)`, die nur Einträge zurückgibt, die den Suchbegriff enthalten und sortiere die Liste mit `sort()` oder `sorted()` und gib das Ergebnis aus

**Schritt 5 – Generator zusammenbauen:**
Rufe im Hauptteil alle Funktionen auf und gib eine kurze Zusammenfassung (Anzahl, Statistik, gefilterte Anzahl) aus

**Bonus:** Füge zufällige Attribute (z.B. Dauer, Intensität) hinzu und prüfe die Daten auf Gültigkeit."""
