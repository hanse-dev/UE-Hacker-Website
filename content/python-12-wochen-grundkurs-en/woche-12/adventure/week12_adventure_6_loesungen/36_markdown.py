"""### Stage 6: Catching Wrong Input – Task 3

Write `parse(text)`: it splits the text with `split()` into `(action, target)` and returns both as a tuple – on a `ValueError` it returns `None`. Print `parse(\"go north\")` and `parse(\"hello\")` (two lines)."""