"""### ⭐⭐⭐⭐☆ Mission 3: The CSV Database

Create a complete CSV management system!

**Step 1 – CSV export:**
Write a list of hero dictionaries (`name`, `class`, `level`) with `csv.DictWriter` to `heroes.csv` (with `newline=\"\"`) and print a confirmation

**Step 2 – CSV import:**
Read the file with `csv.DictReader`, collect the rows into a list, and print the count and the first row

**Step 3 – Search function:**
Write a function that returns only the rows matching a given class, and call it and print the result

**Step 4 – Short report:**
Calculate a small statistic (e.g. average level, count per class) from the read data and print it

**Example code:** `csv.DictWriter`/`csv.DictReader`, `fieldnames=[\"name\", \"class\", \"level\"]`.

**Bonus:** Check that all expected columns are present in the CSV file."""
