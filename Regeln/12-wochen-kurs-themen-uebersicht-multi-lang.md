# 📚 Programmierung 12-Wochen-Kurs: Themenübersicht (Multi-Language)

> **Hinweis:** Diese Vorlage kann für jede Programmiersprache angepasst werden. Ersetze einfach die sprachspezifischen Beispiele und Syntax.

> **Letzte Aktualisierung:** Neue Dateien können direkt im gleichen Ordner erstellt werden!

## 📋 Allgemeine Themenübersicht

### Woche 1: Einführung und erstes Programm
- **Thema:** Grundlagen der Programmierung
- **Die Kern-Lernpunkte:**
  1. Text ausgeben (z.B. `print()` / `console.log()` / `Console.WriteLine()`)
  2. Informationen in Variablen speichern
  3. Texte kombinieren (String-Verkettung)
  4. Kommentare hinzufügen

**Sprachspezifische Beispiele:**
- **Python:** `print("Hallo Welt")`
- **JavaScript:** `console.log("Hallo Welt")`
- **C#:** `Console.WriteLine("Hallo Welt")`

---

### Woche 2: Datentypen und Variablen
- **Thema:** Die Grunddatentypen
- **Die Kern-Lernpunkte:**
  1. Die vier Datentypen: String, Integer, Float, Boolean
  2. Mathematische Operationen und String-Operationen
  3. Typumwandlung und Benutzereingaben

**Sprachspezifische Besonderheiten:**
- **Python:** Dynamische Typisierung, `input()` für Eingaben
- **JavaScript:** Dynamische Typisierung, `prompt()` für Eingaben
- **C#:** Statische Typisierung, `Console.ReadLine()` für Eingaben

---

### Woche 3: Bedingungen (if-else)
- **Thema:** Entscheidungen im Code
- **Die Kern-Lernpunkte:**
  1. if, if-else, if-else if-else Bedingungen
  2. Vergleichs- und logische Operatoren
  3. Verschachtelte Bedingungen und Einrückung/Klammern

**Syntax-Vergleich:**
- **Python:** `if bedingung:` (Einrückung)
- **JavaScript:** `if (bedingung) { }` (geschweifte Klammern)
- **C#:** `if (bedingung) { }` (geschweifte Klammern)

---

### Woche 4: Schleifen (for, while)
- **Thema:** Code wiederholen
- **Die Kern-Lernpunkte:**
  1. for-Schleifen mit Zählern und über Sequenzen
  2. while-Schleifen und break/continue
  3. Verschachtelte Schleifen und Endlosschleifen vermeiden

**Sprachspezifische Syntax:**
- **Python:** `for i in range(10):` / `while bedingung:`
- **JavaScript:** `for (let i = 0; i < 10; i++)` / `while (bedingung)`
- **C#:** `for (int i = 0; i < 10; i++)` / `while (bedingung)`

---

### Woche 5: Funktionen
- **Thema:** Eigene Code-Blöcke erstellen
- **Die Kern-Lernpunkte:**
  1. Funktionen definieren und Parameter
  2. Rückgabewerte und Funktionsaufrufe
  3. Dokumentation und Namenskonventionen

**Syntax-Vergleich:**
- **Python:** `def funktion(param):`
- **JavaScript:** `function funktion(param) { }`
- **C#:** `void Funktion(Typ param) { }`

---

### Woche 6: Listen/Arrays - Grundlegende Operationen
- **Thema:** Daten sammlen und organisieren
- **Die Kern-Lernpunkte:**
  1. Listen/Arrays erstellen und auf Elemente zugreifen
  2. Elemente hinzufügen, entfernen und ändern
  3. Built-in Funktionen und Methoden

**Sprachspezifische Namen:**
- **Python:** `list`
- **JavaScript:** `Array`
- **C#:** `List<T>` oder `Array`

---

### Woche 7: Module und Bibliotheken
- **Thema:** Vorgefertigten Code nutzen
- **Die Kern-Lernpunkte:**
  1. Module/Bibliotheken importieren und verwenden
  2. Mathematische Funktionen nutzen
  3. Zufallszahlen generieren

**Import-Syntax:**
- **Python:** `import math` / `from math import sqrt`
- **JavaScript:** `import math from 'math'` / `const math = require('math')`
- **C#:** `using System.Math;` / `using System.Random;`

---

### Woche 8: Dictionaries/Objects und Tupel
- **Thema:** Schlüssel-Wert-Paare und unveränderliche Daten
- **Die Kern-Lernpunkte:**
  1. Dictionaries/Objects erstellen und mit Schlüsseln arbeiten
  2. Tupel als unveränderliche Datenstrukturen verwenden
  3. Unterschiede und Anwendungsfälle

**Sprachspezifische Namen:**
- **Python:** `dict` und `tuple`
- **JavaScript:** `Object` und `Array` (const)
- **C#:** `Dictionary<TKey, TValue>` und `Tuple<T1, T2>`

---

### Woche 9: Testing
- **Thema:** Code Qualität sicherstellen
- **Die Kern-Lernpunkte:**
  1. Unit Tests schreiben
  2. Assert-Statements und Test-Fälle
  3. Test-Driven Development Grundlagen

**Testing-Frameworks:**
- **Python:** `unittest` oder `pytest`
- **JavaScript:** `Jest` oder `Mocha`
- **C#:** `NUnit` oder `xUnit`

---

### Woche 10: Dateioperationen
- **Thema:** Daten speichern und laden
- **Die Kern-Lernpunkte:**
  1. Dateien lesen und schreiben
  2. CSV- und JSON-Dateien verarbeiten
  3. Fehlerbehandlung beim Dateizugriff

**Sprachspezifische APIs:**
- **Python:** `open()` und `json`-Modul
- **JavaScript:** `fs`-Modul (Node.js) oder `File API` (Browser)
- **C#:** `System.IO` und `System.Text.Json`

---

### Woche 11: Grafische Projekte
- **Thema:** Visuelle Programmierung
- **Die Kern-Lernpunkte:**
  1. Grafik-Bibliothek importieren und einrichten
  2. Formen zeichnen und bewegen
  3. Farben und interaktive Grafik

**Grafik-Bibliotheken:**
- **Python:** `turtle`, `pygame` oder `tkinter`
- **JavaScript:** `Canvas API` oder `p5.js`
- **C#:** `System.Drawing` oder `Unity`

---

### Woche 12: Abschlussprojekt
- **Thema:** Alles kombinieren
- **Die Kern-Lernpunkte:**
  1. Eigenes Projekt planen und strukturieren
  2. Alle gelernten Konzepte anwenden
  3. Projekt präsentieren und dokumentieren

**Projekt-Ideen nach Sprache:**
- **Python:** Datenanalyse, Web-Scraping, CLI-Tool
- **JavaScript:** Web-App, Browser-Spiel, API-Client
- **C#:** Desktop-App, Spiel mit Unity, Web-Service

---

## 🎯 Anpassungshinweise für andere Sprachen

### Für neue Programmiersprachen anpassen:

1. **Syntax-Beispiele:** Ersetze die Code-Beispiele mit der Syntax der Zielsprache
2. **Spezifische Konzepte:** Ergänze sprachspezifische Besonderheiten
3. **Standardbibliotheken:** Passe die Bibliotheksnamen an
4. **Namenskonventionen:** Beachte die Konventionen der Sprache
5. **Best Practices:** Füge sprachspezifische Best Practices hinzu

### Beispiel für eine neue Sprache anlegen:

```markdown
### Woche 1: Einführung und erstes Programm
- **Thema:** Grundlagen der Programmierung
- **Die Kern-Lernpunkte:**
  1. Text ausgeben (z.B. `println!("Hallo Welt")`)
  2. Informationen in Variablen speichern
  3. Texte kombinieren
  4. Kommentare hinzufügen

**Rust-spezifisch:**
- Statische Typisierung mit Typ-Inferenz
- `let` für unveränderliche Variablen, `let mut` für veränderliche
- `println!` ist ein Makro
```

---

## 📊 Sprachvergleichstabelle

| Konzept | Python | JavaScript | C# | Java | Rust |
|---------|---------|------------|----|------|------|
| **Ausgabe** | `print()` | `console.log()` | `Console.WriteLine()` | `System.out.println()` | `println!()` |
| **Variablen** | `x = 5` | `let x = 5` | `int x = 5` | `int x = 5` | `let x = 5` |
| **Strings** | `"text"` | `"text"` | `"text"` | `"text"` | `"text"` |
| **If-Statement** | `if x > 0:` | `if (x > 0) {}` | `if (x > 0) {}` | `if (x > 0) {}` | `if x > 0 {}` |
| **For-Loop** | `for i in range(10):` | `for (let i=0; i<10; i++)` | `for (int i=0; i<10; i++)` | `for (int i=0; i<10; i++)` | `for i in 0..10` |
| **Funktion** | `def f():` | `function f() {}` | `void F() {}` | `void f() {}` | `fn f() {}` |
| **Array/Liste** | `[1,2,3]` | `[1,2,3]` | `new List<int>{1,2,3}` | `new int[]{1,2,3}` | `vec![1,2,3]` |

---

## 🚀 Verwendung dieser Vorlage

1. **Kopieren:** Kopiere diese Datei für deinen neuen Kurs
2. **Umbenennen:** Benenne sie um, z.B. `csharp-12-wochen-kurs-themen.md`
3. **Anpassen:** Ersetze die sprachspezifischen Beispiele
4. **Ergänzen:** Füge sprachspezifische Besonderheiten hinzu
5. **Verwenden:** Nutze sie als Leitfaden für deine Kurs-Erstellung

Diese Vorlage sorgt für Konsistenz über verschiedene Programmiersprachen hinweg, während sie gleichzeitig Flexibilität für sprachspezifische Anpassungen bietet.
