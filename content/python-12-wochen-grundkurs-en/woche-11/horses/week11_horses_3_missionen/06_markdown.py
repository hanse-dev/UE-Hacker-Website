"""### ⭐⭐⭐⭐☆ Mission 3: The Champion Register

The tournament office needs a system for champion horses!

**Step 1 – Create class Champion:**
Create `class Champion:` with `__init__(self, name, breed, points)`; assign the attributes

**Step 2 – Write Magic Methods:**
`__str__(self)`: Return a readable string, e.g. `\"🏆 {name} ({breed}), Points: {points}\"` and `__add__(self, other)`: Return the sum of the points of both champions

**Step 3 – Test Magic Methods:**
Create two Champion objects and use a function, `len(champion)` and `champion1 + champion2`


> 💡 **Tip:** You decide how to implement it – there is no single correct way!
**Bonus:** Add `__eq__(self, other)` and `__lt__(self, other)` for comparisons by points."""
