"""### ⭐⭐⭐☆☆ Mission 2: The Coordinates Master

You use tuples for 3D coordinates and combine them with dictionaries.

**Step 1 – Coordinates as tuples:**
Create at least two 3D coordinates as tuples (e.g. `pos1 = (100, 200, 50)`) and print the tuples and their length with `len(pos1)`

**Step 2 – Tuple unpacking:**
Use unpacking: `x, y, z = pos1` and print x, y, z, then create a second coordinate and unpack it as well

**Step 3 – Position dictionary:**
Create a dictionary `positions` where keys are names (strings) and values are coordinate tuples (e.g. `{\"Alpha\": (10, 20, 0), \"Beta\": (30, 40, 5)}`), print the dictionary and access one position (e.g. `positions[\"Alpha\"]`)

**Step 4 – Distance (simplified):**
Write a simple function `distance(coord1, coord2)` that e.g. computes the sum of absolute differences per axis (or Euclidean distance with `**0.5`), call it for two of your coordinates and print the result

**Bonus:** Add 4D coordinates (x, y, z, t) as tuples and extend the distance calculation."""
