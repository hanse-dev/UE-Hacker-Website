"""### ⭐⭐⭐⭐☆ Boss-Quest 1: Der Schatzkammer-Katalog

Baue ein System, das automatisch Schätze erzeugt, filtert und sortiert.

**Schritt 1 – Schätze erzeugen:**
Schreibe `generiere_schatz(material, seltenheit)`, die eine Liste mit Einträgen zurückgibt (z.B. Schatznamen je nach Material/Seltenheit) und gib die Liste mit `return` zurück

**Schritt 2 – Statistik berechnen:**
Erstelle `berechne_schatz_statistik(schatzliste)`, die Länge und optional Durchschnitt zurückgibt und gib die Werte als Dictionary oder ausgedruckt zurück

**Schritt 3 – Mehrere Schätze anlegen:**
Erstelle eine Liste `schaetze` und füge mit einer Schleife mindestens 10 Schätze hinzu (z.B. mit `generiere_schatz(\"Gold\", i)`) und nutze `append()` für jeden Eintrag

**Schritt 4 – Schätze filtern und sortieren:**
Schreibe `filtere_schaetze(schatzliste, suchbegriff)`, die nur Einträge zurückgibt, die den Suchbegriff enthalten und sortiere die Liste mit `sort()` oder `sorted()` und gib das Ergebnis aus

**Schritt 5 – Katalog zusammenbauen:**
Rufe im Hauptteil alle Funktionen auf und gib eine kurze Zusammenfassung (Anzahl, Statistik, gefilterte Anzahl) aus

**Bonus:** Füge zufällige Attribute (z.B. Gewicht, Wert) hinzu und prüfe die Daten auf Gültigkeit."""
