"""### ⭐⭐⭐☆☆ Mission 2: The JSON Wizard

Create a JSON magic system!

**Step 1 – Create hero data:**
Create a dictionary with hero data (`name`, `class`, `level`, `hit_points`) and print it

**Step 2 – JSON export:**
Save the dictionary with `json.dump(..., indent=2)` to `hero.json` and print a confirmation

**Step 3 – JSON import:**
Load the file again with `json.load()`, check e.g. `data[\"name\"]`, and print the loaded data

**Step 4 – Short statistics:**
Print a short summary (e.g. level, hit points) from the loaded data

**Example code:** `hero = {...}`, `json.dump(hero, f, indent=2)`, `json.load(f)`.

**Bonus:** Add simple validation (check that all expected keys are present)."""
