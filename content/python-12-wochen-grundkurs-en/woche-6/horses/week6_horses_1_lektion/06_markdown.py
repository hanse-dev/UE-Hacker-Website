"""## 📦 Collection Technique 2: Managing Elements – The Stable Master

**The most important list methods:**

**Adding elements:**
- `append(element)` – Adds at the end
- `insert(index, element)` – Inserts at a specific position

**Removing elements:**
- `remove(element)` – Removes the first occurrence
- `pop(index)` – Removes and returns an element
- `clear()` – Empties the entire list"""
