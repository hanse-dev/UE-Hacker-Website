"""### ⭐⭐☆☆☆ Mission 1: A new room

There should be a secret room behind the hall!

**Step 1 – New room:**
Add a room `"library"` to the dictionary `world`. It needs a `"description"`, the `"exits"` (a way back `"south"` to the room `"hall"`), a list `"items"` with one item (e.g. `Spellbook`) and `"enemy": None`.

**Step 2 – Connect:**
Add an exit `"north"` to the room `"hall"` that leads to the new room.

**Step 3 – Test:**
Create a `Player` in `"hall"`, let them go `north` and take the item.

**Bonus:** Give the room a second door that leads to `"spring"` (don't forget the way back!)."""