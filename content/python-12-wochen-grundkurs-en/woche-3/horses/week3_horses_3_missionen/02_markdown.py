"""### ⭐⭐☆☆☆ Mission 1: The Stable Guard

The stable guard checks who is allowed into the stable!

**Step 1 – Collect access data:**
Create a variable for age (integer) and create a variable for helmet ownership (Boolean True/False)

**Step 2 – Write the access rule:**
Check with if whether age >= 12 OR helmet present OR permission and use the `or` operator to combine them

**Step 3 – Add a rejection message:**
Use else for the case where no condition applies and print \"Access denied\"

**Bonus:** Only require a helmet for riders under 16!"""
