"""### ⭐⭐⭐⭐⭐ Boss-Quest 2: Der Stall-Manager

Verwalte einen kompletten Stall mit Boxen, Futter und Reitern über mehrere Funktionen.

**Schritt 1 – Boxen erzeugen:**
Erstelle eine Funktion `erstelle_box(box_id, groesse)`, die ein Dictionary mit ID, Größe und aktueller Futtermenge zurückgibt und lege eine Liste mit mehreren Boxen an

**Schritt 2 – Futter verteilen:**
Schreibe eine Funktion `verteile_futter(boxen, totales_futter)`, die die Futtermenge proportional auf die Boxen verteilt und aktualisiere dazu die Futtereinträge in den Box-Dictionaries

**Schritt 3 – Schichtplanung:**
Erstelle eine Funktion `plane_stall_schichten(anzahl_reiter, schichten)`, die eine einfache Verteilung der Reiter auf die Schichten berechnet und gib eine Liste mit Reitern pro Schicht zurück

**Schritt 4 – Stall-Übersicht erzeugen:**
Schreibe eine Funktion `erzeuge_stallbericht(boxen, futter, reiter)`, die eine Zusammenfassung als Text zurückgibt und nutze dabei Informationen aus den Boxen und den Parametern

**Bonus:** Ergänze eine kleine Notfall-Funktion, die bei einer Box mit zu wenig Futter eine Warnung zurückgibt."""
