"""## Collection Spell 4: break and continue – Controlling Loops

When you loop through a list, you can control the loop in a targeted way:

- **`break`** – Exit the loop immediately (e.g. when you've found what you're looking for)
- **`continue`** – Skip the current iteration and continue with the next one

```python
for item in list_:
    if item == \"Target\":
        break       # found – stop
    if item == \"Skip\":
        continue    # ignore this element
    print(item)
```"""
