"""### ⭐⭐☆☆☆ Mission 1: Der Zauber-Lehrling

In der Bibliothek der Wiederholungen lernst du deine ersten wiederverwendbaren Zauberformeln.

**Schritt 1 – Begrüßungszauber sprechen:**
Erstelle eine Funktion `begruesse_magier()`, die zwei Begrüßungszeilen für den Magier ausgibt und rufe die Funktion mindestens zweimal auf

**Schritt 2 – Mana-Berechnung als Zauber:**
Erstelle eine Funktion `berechne_mana(level, basis_mana)`, die `level * basis_mana` berechnet und gib den Wert mit `return` zurück

**Schritt 3 – Zauber-Verfügbarkeit prüfen:**
Schreibe eine Funktion `ist_zauber_moeglich(mana_kosten, aktuelles_mana)`, die `True` zurückgibt, wenn genug Mana vorhanden ist, sonst `False` und nutze die Ergebnisse in if-Bedingungen und gib passende Nachrichten aus

**Schritt 4 – Formeln kombinieren:**
Erstelle einen kleinen Haupt-Block, der: und den Magier begrüßt,

**Bonus:** Erstelle eine Funktion `generiere_zauber()`, die zufällig einen Zauber (z.B. \"Feuerball\", \"Heilung\", \"Schild\") zurückgibt."""
