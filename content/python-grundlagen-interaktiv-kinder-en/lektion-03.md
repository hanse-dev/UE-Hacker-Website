# Numbers and Maths 🔢

Python can calculate! Use `+`, `-`, `*` and `/` just like at school.

```python
apples = 5
pears = 3
fruit = apples + pears
print(fruit)  # prints 8
```

And with variables it gets even more fun:

```python
points = 10 * 3
print(points)  # prints 30
```
