"""### ⭐⭐☆☆☆ Mission 1: Der Daten-Archivar

In den Daten-Archiven der Nebula-7 erstellst du dein erstes Dictionary für Schiffsdaten.

**Schritt 1 – Schiff-Dictionary anlegen:**
Erstelle ein Dictionary `schiff` mit Schlüsseln z.B. `name`, `typ`, `crew`, `geschwindigkeit` und passenden Werten und gib das Dictionary mit `print()` aus

**Schritt 2 – Werte ändern und ergänzen:**
Ändere den Wert von `crew` (z.B. auf eine neue Zahl) und füge einen neuen Schlüssel `kapitaen` mit einem Wert hinzu

**Schritt 3 – Planeten-Liste aus Dictionaries:**
Erstelle eine Liste mit 3 Elementen; jedes Element ist ein Dictionary mit z.B. `name` und `entfernung` (oder ähnlichen Schlüsseln) und gib die Liste und die Werte eines Planeten (z.B. `planeten[0][\"name\"]`) aus

**Schritt 4 – Zugriff und Ausgabe:**
Greife mit `schiff[\"name\"]` und `schiff.get(\"typ\")` auf Werte zu und gib sie aus und gib eine kurze übersichtliche Zusammenfassung (z.B. „Schiff X, Typ Y, Crew Z“) aus

**Bonus:** Erstelle ein verschachteltes Dictionary (z.B. Schiff mit Unter-Dictionary „systeme“)."""
