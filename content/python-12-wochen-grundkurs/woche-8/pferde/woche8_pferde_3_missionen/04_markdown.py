"""### ⭐⭐⭐☆☆ Mission 2: Der Koordinaten-Reiter

Du nutzt Tupel für 2D-Koordinaten (Reitwege) und kombinierst sie mit Dictionaries.

**Schritt 1 – Koordinaten als Tupel:**
Erstelle mindestens zwei 2D-Koordinaten als Tupel (z.B. `pos1 = (100, 200)` für Wegpunkte) und gib die Tupel und die Länge mit `len(pos1)` aus

**Schritt 2 – Tupel-Unpacking:**
Nutze Unpacking: `x, y = pos1` und gib x, y aus und erstelle eine zweite Koordinate und unpacke sie ebenfalls

**Schritt 3 – Positions-Dictionary:**
Erstelle ein Dictionary `positionen`, in dem Schlüssel Namen (Strings, z.B. „Stall“, „Reitplatz“) und Werte Koordinaten-Tupel sind (z.B. `{\"Stall\": (0, 0), \"Reitplatz\": (50, 30)}`) und gib das Dictionary aus und greife auf eine Position zu (z.B. `positionen[\"Stall\"]`)

**Schritt 4 – Abstand (vereinfacht):**
Schreibe eine einfache Funktion `abstand(koord1, koord2)`, die z.B. die euklidische Distanz für 2D berechnet: √((x1-x2)² + (y1-y2)²) und rufe sie für zwei deiner Koordinaten auf und gib das Ergebnis aus

**Bonus:** Füge 3D-Koordinaten (x, y, z) als Tupel hinzu und erweitere die Abstandsberechnung."""
