"""### ⭐⭐☆☆☆ Mission 1: A new room

There should be a secret room next to the corridor!

**Step 1 – New room:**
Add a room `"bridge"` to the dictionary `world`. It needs a `"description"`, the `"exits"` (a way back `"south"` to the room `"corridor"`), a list `"items"` with one item (e.g. `Datapad`) and `"enemy": None`.

**Step 2 – Connect:**
Add an exit `"north"` to the room `"corridor"` that leads to the new room.

**Step 3 – Test:**
Create a `Player` in `"corridor"`, let them go `north` and take the item.

**Bonus:** Give the room a second door that leads to `"lab"` (don't forget the way back!)."""