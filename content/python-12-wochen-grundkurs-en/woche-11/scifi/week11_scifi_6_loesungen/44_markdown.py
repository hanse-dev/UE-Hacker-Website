"""## 🐛 Debug Quest

### Debug Quest – Task 1

🐛 Bug #1 – Goal: The program should print Nova. What is wrong?

**Explanation:** A child's own `__init__` replaces the parent's. Without `super().__init__(name)` `self.name` is never set."""