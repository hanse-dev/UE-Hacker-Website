"""### ⭐⭐⭐⭐☆ Mission 3: The CSV Database

Create a complete CSV management system!

**Step 1 – CSV export:**
Write a list of horse dictionaries (`name`, `breed`, `age`) with `csv.DictWriter` to `horses.csv` (with `newline=\"\"`) and print a confirmation

**Step 2 – CSV import:**
Read the file with `csv.DictReader`, collect the rows into a list, and print the count and the first row

**Step 3 – Search function:**
Write a function that returns only the rows matching a given breed, and call it and print the result

**Step 4 – Short report:**
Calculate a small statistic (e.g. average age, count per breed) from the read data and print it

**Bonus:** Check that all expected columns are present in the CSV file."""
