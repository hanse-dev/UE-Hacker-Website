"""### ⭐⭐☆☆☆ Mission 1: Der Stallarchivar

Auf dem Reiterhof Sonnental erstellst du deine erste Stallkarte (Dictionary) für Pferdedaten.

**Schritt 1 – Pferde-Stallkarte anlegen:**
Erstelle ein Dictionary `pferd` mit Schlüsseln z.B. `name`, `rasse`, `alter`, `groesse` und passenden Werten und gib das Dictionary mit `print()` aus

**Schritt 2 – Werte ändern und ergänzen:**
Ändere den Wert von `alter` (z.B. auf eine neue Zahl) und füge einen neuen Schlüssel `besitzer` mit einem Wert hinzu

**Schritt 3 – Pferde-Liste aus Dictionaries:**
Erstelle eine Liste mit 3 Elementen; jedes Element ist ein Dictionary mit z.B. `name`, `rasse`, `alter` und gib die Liste und die Werte eines Pferdes (z.B. `pferde[0][\"name\"]`) aus

**Schritt 4 – Zugriff und Ausgabe:**
Greife mit `pferd[\"name\"]` und `pferd.get(\"rasse\")` auf Werte zu und gib sie aus und gib eine kurze übersichtliche Zusammenfassung (z.B. „Pferd X, Rasse Y, Alter Z“) aus

**Bonus:** Erstelle ein verschachteltes Dictionary (z.B. Pferd mit Unter-Dictionary „gesundheit“)."""
