"""### ⭐⭐⭐☆☆ Mission 2: The Artifact Master

You use tuples for artifacts (immutable magic items) and combine them with dictionaries.

**Step 1 – Artifacts as tuples:**
Create at least two \"artifacts\" as tuples (e.g. `artifact1 = (\"Sword\", 10, \"Fire\")` for name, strength, element) and print the tuples and their length with `len(artifact1)`

**Step 2 – Tuple unpacking:**
Use unpacking: `name, strength, element = artifact1` and print name, strength, element; then create a second artifact tuple and unpack it as well

**Step 3 – Artifact dictionary:**
Create a dictionary `artifacts` where keys are names (strings) and values are tuples (e.g. `{\"Fire Staff\": (\"Staff\", 15, \"Fire\"), \"Ice Ring\": (\"Ring\", 5, \"Ice\")}`) and print the dictionary and access one artifact (e.g. `artifacts[\"Fire Staff\"]`)

**Step 4 – Comparison (simplified):**
If your tuples contain numbers (e.g. strength, level), write a simple function that compares two such tuples (e.g. difference in strengths) or calculates the \"distance\" between two coordinate tuples and print the result

**Bonus:** Add \"4D artifacts\" (tuples with 4 values) and extend the comparison or distance calculation."""
