"""### ⭐⭐☆☆☆ Mission 1: The Treasure Collector

In the treasure vault you create your first organised treasure list.

**Step 1 – Create the treasure list:**
Create a list `treasures` with 5 different treasures (e.g. \"Gold\", \"Crystal\", \"Amulet\") and print the list with `print()`, using square brackets `[]`

**Step 2 – Add treasures:**
Use `append()` to add three more treasures and print the list afterwards; optional: use `insert(0, \"Name\")` to put a treasure at the beginning

**Step 3 – Remove a treasure and find a position:**
Remove a treasure with `remove(\"Name\")` and find the position of another with `index(\"Name\")`; print the current list and the found position

**Step 4 – Sort and output:**
Sort the treasures with `sort()` and print the list along with `len(treasures)`

**Bonus:** Create a nested list with treasure details (name, value, rarity)."""
