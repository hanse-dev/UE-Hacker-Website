"""### ⭐⭐⭐⭐☆ Boss Quest 1: The Rank Hierarchy

The fleet has cadets, officers and captains – each with different authorities!

**Step 1 – Base class:**
Write a class `CrewMember` with name and rank. Add a method `show_permissions()`.

**Step 2 – Ranks:**
Derive 3 child classes (Cadet, Officer, Captain). Each overrides `show_permissions()`.

**Step 3 – Briefing:**
Create one member of each rank. Call `show_permissions()` for all.

**Bonus:** Add `__str__` for a nice output."""
