"""### ⭐⭐⭐⭐☆ Boss Quest 3: The Tournament Database

Manage tournaments in a list: create, search, filter and evaluate.

**Step 1 – Create a tournament:**
Create `create_tournament(name, discipline, difficulty)`, which returns a dictionary with \"name\", \"discipline\", \"difficulty\" (and optionally \"status\") and return the dictionary with `return`

**Step 2 – Add a tournament to the list:**
Create a list `tournaments` and write `add_tournament(tournament, tournament_list)`, which adds with `append()` and prints a confirmation

**Step 3 – Search tournaments:**
Write `search_tournaments(tournament_list, search_term)`, which returns all tournaments whose name or discipline contains the search term

**Step 4 – Filter by status:**
Create `filter_by_status(tournament_list, status)` and return the filtered list

**Step 5 – Output statistics:**
Write `tournament_stats(tournament_list)`, which prints the count and optionally the distribution by discipline; call all functions in the main section

**Bonus:** Add a \"result\" or \"prize\" field to each tournament and print a success statistic."""
