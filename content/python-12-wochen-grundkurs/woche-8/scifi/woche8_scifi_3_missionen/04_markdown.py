"""### ⭐⭐⭐☆☆ Mission 2: Der Koordinaten-Meister

Du nutzt Tupel für 3D-Koordinaten und kombinierst sie mit Dictionaries.

**Schritt 1 – Koordinaten als Tupel:**
Erstelle mindestens zwei 3D-Koordinaten als Tupel (z.B. `pos1 = (100, 200, 50)`) und gib die Tupel und die Länge mit `len(pos1)` aus

**Schritt 2 – Tupel-Unpacking:**
Nutze Unpacking: `x, y, z = pos1` und gib x, y, z aus und erstelle eine zweite Koordinate und unpacke sie ebenfalls

**Schritt 3 – Positions-Dictionary:**
Erstelle ein Dictionary `positionen`, in dem Schlüssel Namen (Strings) und Werte Koordinaten-Tupel sind (z.B. `{\"Alpha\": (10, 20, 0), \"Beta\": (30, 40, 5)}`) und gib das Dictionary aus und greife auf eine Position zu (z.B. `positionen[\"Alpha\"]`)

**Schritt 4 – Abstand (vereinfacht):**
Schreibe eine einfache Funktion `abstand(koord1, koord2)`, die z.B. die Summe der absoluten Differenzen pro Achse berechnet (oder euklidische Distanz mit `**0.5`) und rufe sie für zwei deiner Koordinaten auf und gib das Ergebnis aus

**Bonus:** Füge 4D-Koordinaten (x, y, z, t) als Tupel hinzu und erweitere die Abstandsberechnung."""
