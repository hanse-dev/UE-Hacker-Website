# Lists – Many Values at Once 📋

A list stores multiple values in a row. The first entry has index 0.

```python
animals = ["Dog", "Cat", "Mouse"]
print(animals[0])   # prints "Dog"
print(animals[-1])  # prints "Mouse" (last element!)
```

Lists can hold anything: text, numbers, even other lists!
