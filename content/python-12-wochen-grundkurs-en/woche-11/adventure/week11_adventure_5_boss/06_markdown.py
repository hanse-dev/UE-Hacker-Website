"""### ⭐⭐⭐⭐☆ Boss Quest 3: The Polymorphic Animal System

All animals can speak – but each says something different!

**Step 1 – Base class:**
Write a class `Animal` with name. Add a method `make_sound()` that prints a general message.

**Step 2 – Animal types:**
Derive 4 animals (e.g. Dog, Cat, Dragon, Owl). Each overrides `make_sound()` with its typical sound.

**Step 3 – Animal Parade:**
Create one of each animal and call `make_sound()` for all – show that Python automatically picks the right version.

**Bonus:** Add `__str__` and `__len__` (`__len__` returns the length of the name)."""
