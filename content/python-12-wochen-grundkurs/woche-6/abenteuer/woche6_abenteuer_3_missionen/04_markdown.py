"""### ⭐⭐⭐☆☆ Mission 2: Der Inventar-Manager

Du verwaltest dein Inventar mit Listen und kleinen Hilfsfunktionen.

**Schritt 1 – Inventarliste anlegen:**
Erstelle eine Liste `inventar` mit Waffen und Rüstungen (z.B. \"Schwert\", \"Schild\", \"Trank\") und gib die Liste und `len(inventar)` aus

**Schritt 2 – Items hinzufügen:**
Nutze `append()` und `insert()`, um Items am Ende und an einer Position (z.B. 0) einzufügen und gib die aktualisierte Liste aus

**Schritt 3 – Items entfernen und suchen:**
Entferne mit `remove()` und `pop()`, finde mit `index()` eine Position und prüfe mit `in` und gib die Liste und die gefundene Position aus

**Schritt 4 – Statistik ausgeben:**
Gib Anzahl der Items, die aktuelle Liste und optional eine kurze Statuszeile aus

**Bonus:** Speichere zu jedem Item einen Wert (z.B. in einer zweiten Liste) und gib eine sortierte Übersicht nach Wert aus."""
