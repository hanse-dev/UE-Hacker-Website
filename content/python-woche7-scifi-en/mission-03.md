# ⭐⭐⭐⭐☆ Mission 3: The Code Forge

Forge an access code for the airlock and check whether it is the day or the night shift.

**Bonus (optional, not checked):** Use random.seed() before creating the key so the same key is created reproducibly.
