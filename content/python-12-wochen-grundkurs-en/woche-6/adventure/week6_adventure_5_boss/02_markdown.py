"""### ⭐⭐⭐⭐☆ Boss Quest 1: The Treasure Vault Catalogue

Build a system that automatically generates, filters, and sorts treasures.

**Step 1 – Generate treasures:**
Write `generate_treasure(material, rarity)` that returns a list with entries (e.g. treasure names depending on material/rarity) and return the list with `return`

**Step 2 – Calculate statistics:**
Create `calculate_treasure_stats(treasure_list)` that returns the length and optionally the average, and print or return the values as a dictionary

**Step 3 – Create multiple treasures:**
Create a list `treasures` and add at least 10 treasures using a loop (e.g. with `generate_treasure(\"Gold\", i)`) and use `append()` for each entry

**Step 4 – Filter and sort treasures:**
Write `filter_treasures(treasure_list, search_term)` that returns only entries containing the search term, sort the list with `sort()` or `sorted()`, and print the result

**Step 5 – Assemble the catalogue:**
Call all functions in the main section and print a short summary (count, stats, filtered count)

**Bonus:** Add random attributes (e.g. weight, value) and validate the data."""
