"""### Stage 7: Saving and the Finale – Task 3

All building blocks are given. Create the player, take Keycard, save it with `save(player, \"test.json\")`, load it with `load(\"test.json\")` and print exactly Position: airlock (position of the loaded player)."""