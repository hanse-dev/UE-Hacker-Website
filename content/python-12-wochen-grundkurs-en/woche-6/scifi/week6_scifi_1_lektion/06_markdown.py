"""## 📦 Data Collection 2: Managing Elements – The Data Curator

**The most important list methods:**

**Adding elements:**
- `append(element)` – Adds at the end
- `insert(index, element)` – Inserts at a specific position

**Removing elements:**
- `remove(element)` – Removes first occurrence
- `pop(index)` – Removes and returns element
- `clear()` – Empties the entire list"""
