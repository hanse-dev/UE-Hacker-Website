"""## Lesson 3: __init__

**What it is:** The constructor *(= special method that runs automatically when an object is created)* is a special method that is automatically executed when a horse is born.

**Important:** `__init__` always has `self` as its first parameter! (Why `self` is needed is explained in the next section.)"""
