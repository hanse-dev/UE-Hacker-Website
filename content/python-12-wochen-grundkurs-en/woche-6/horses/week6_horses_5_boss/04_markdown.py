"""### ⭐⭐⭐⭐⭐ Boss Quest 2: The Stable Manager

Manage stalls and horses with lists and simple functions.

**Step 1 – Create stalls:**
Create `create_stalls(num_stalls, horses_per_stall)`, which returns a nested list (each stall is a list with placeholders like \"empty\") and return the nested list

**Step 2 – Place horses:**
Write `place_horse(stalls, stall_index, horse_name)`, which adds a horse at the given stall position and use index access to update the list

**Step 3 – Distribute feed:**
Create `distribute_feed(stalls, total_feed)`, which distributes feed evenly or by occupancy to the stalls and return a list with feed per stall

**Step 4 – Display overview:**
Write `show_overview(stalls)`, which prints all stalls and their horses in a formatted way and print a short summary at the end (number of stalls, total places)

**Bonus:** Simulate random care tasks (e.g. \"muck out stall\") and a function that marks them as done."""
