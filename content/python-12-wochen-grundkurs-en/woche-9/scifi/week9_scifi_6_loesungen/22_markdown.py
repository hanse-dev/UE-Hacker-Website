"""### Data Log 4: JSON text: dumps and loads – Task 3

Pack {\"pos\": (3, 4)} into text with json.dumps() and read it back with json.loads() (into back). Print exactly List: True (back[\"pos\"] == [3, 4]) and on the next line Tuple: False (back[\"pos\"] == (3, 4))."""