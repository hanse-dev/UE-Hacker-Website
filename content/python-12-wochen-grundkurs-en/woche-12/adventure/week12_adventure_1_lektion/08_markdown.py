"""Now the **player** itself! It remembers where the player is, how many hit points they have and what is in their bag. The function `go()` from Stage 2 becomes a **method** (Week 10) – it changes `self.position` directly.

Note that `self.inventory = []` is inside `__init__`. That way **every** player gets their own bag."""