"""### ⭐⭐☆☆☆ Mission 1: The Guardian Seal

The first gate is locked with three guardian seals. Each seal only opens once you summon (import) the matching guardian and prove its power.

**Step 1 – Summon the three guardians:**
Import the modules `random`, `time` and `string` (use an alias with `as` for at least one) and print a sample from each (e.g. `random.randint(1, 6)`, `len(string.ascii_letters)`, `time.time()`)

**Step 2 – The Time Guardian's waiting ritual:**
Generate a random waiting time with `random.uniform(1, 3)`, use `time.sleep()` with it, and print a message before and after the wait (e.g. \"The seal begins to glow …\")

**Step 3 – The Word Guardian's rune fragment:**
Draw a random letter from `string.ascii_letters` with `random.choice()` and print it as a \"rune fragment\"

**Step 4 – Open the seal:**
Print a closing message that sums up all three proofs (e.g. \"All three guardians are defeated – the seal opens!\")

**Bonus:** Use `from datetime import datetime` to add a timestamp message showing when you opened the seal."""
