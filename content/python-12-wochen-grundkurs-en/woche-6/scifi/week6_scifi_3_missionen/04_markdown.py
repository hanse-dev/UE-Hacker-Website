"""### ⭐⭐⭐☆☆ Mission 2: The System Manager

You manage the ship systems of the space station using lists and small helper functions.

**Step 1 – Create system list:**
Create a list `systems` with different ship systems (e.g. \"Propulsion\", \"Shield\", \"Weapons\") and print the list and the number of systems with `len()`

**Step 2 – Add systems:**
Write a function `add_system(list_, name)` or use `append()` and `insert()` directly to add systems, and add at least one system at the end and one at a specific position (e.g. index 0)

**Step 3 – Remove and search systems:**
Remove a system with `remove()` and one with `pop()` (e.g. last element) and find the position of a specific system with `index()`

**Step 4 – Print statistics:**
Print the number of systems, the current list, and optionally a short status line

**Bonus:** Store a priority for each system (e.g. in a second list or as a number) and print a sorted overview by priority."""
