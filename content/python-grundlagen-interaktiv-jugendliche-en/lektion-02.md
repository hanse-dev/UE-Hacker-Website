# Variables

Variables store values under a name. You can overwrite them at any time.

```python
username = "alex"
score = 1500
print(username)
print(score)
```

Variable names: lowercase letters, underscores for spaces (`my_name`). No numbers at the start.
