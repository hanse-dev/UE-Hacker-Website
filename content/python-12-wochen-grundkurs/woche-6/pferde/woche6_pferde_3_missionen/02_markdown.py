"""### ⭐⭐☆☆☆ Mission 1: Der Pferdesammler

Auf dem Reiterhof erstellst du deine erste geordnete Pferdeliste.

**Schritt 1 – Pferdeliste anlegen:**
Erstelle eine Liste `pferde` mit 5 verschiedenen Pferdenamen (z.B. \"Thunder\", \"Luna\", \"Storm\") und gib die Liste mit `print()` aus und nutze eckige Klammern `[]`

**Schritt 2 – Pferde ergänzen:**
Füge mit `append()` drei weitere Pferde an und gib die Liste danach aus und optional: Nutze `insert(0, \"Name\")`, um ein Pferd an den Anfang zu setzen

**Schritt 3 – Pferd entfernen und Position finden:**
Entferne mit `remove(\"Name\")` ein Pferd und finde mit `index(\"Name\")` die Position eines anderen und gib die aktuelle Liste und die gefundene Position aus

**Schritt 4 – Sortieren und Ausgabe:**
Sortiere die Pferde mit `sort()` und gib die Liste sowie `len(pferde)` aus

**Bonus:** Erstelle eine verschachtelte Liste mit Pferde-Details (Name, Rasse, Alter)."""
