"""### ⭐⭐⭐⭐☆ Mission 3: The CSV Database

Create a complete CSV management system!

**Step 1 – CSV export:**
Create a list of dictionaries for crew members (e.g. `name`, `rank`, `age`) with at least 3 entries and import `csv` and write the data with `csv.DictWriter` to a file `crew.csv` (with header row)

**Step 2 – CSV import:**
Read the file with `csv.DictReader` and collect all rows in a list and print the number of entries read and the first row

**Step 3 – Search function:**
Write a function that reads the CSV file and returns all rows where the \"rank\" column equals the given rank and call the function for one rank and print the found entries

**Step 4 – Short report:**
Calculate a simple statistic from the read data (e.g. average age, count per rank) and print it

> 💡 **Tip:** You decide how to implement it – there is no single right way!

**Bonus:** Check when reading whether all expected columns are present (simple validation)."""
