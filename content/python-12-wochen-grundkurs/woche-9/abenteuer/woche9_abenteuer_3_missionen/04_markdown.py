"""### ⭐⭐⭐☆☆ Mission 2: Der JSON-Zauberer

Erstelle ein JSON-Zaubersystem!

**Schritt 1 – Helden-Daten anlegen:**
Erstelle ein Dictionary mit Helden-Daten (`name`, `klasse`, `level`, `lebenspunkte`) und gib es aus

**Schritt 2 – JSON-Export:**
Speichere das Dictionary mit `json.dump(..., indent=2)` in `held.json` und gib eine Bestätigung aus

**Schritt 3 – JSON-Import:**
Lade die Datei wieder mit `json.load()`, prüfe z.B. `daten[\"name\"]` und gib die geladenen Daten aus

**Schritt 4 – Kurze Statistik:**
Gib eine kurze Statistik (z.B. Level, Lebenspunkte) aus den geladenen Daten aus

**Beispiel-Code:** `held = {...}`, `json.dump(held, f, indent=2)`, `json.load(f)`.

**Bonus:** Baue eine einfache Validierung ein (prüfe, ob alle erwarteten Schlüssel vorhanden sind)."""
