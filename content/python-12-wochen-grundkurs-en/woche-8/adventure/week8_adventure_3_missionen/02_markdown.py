"""### ⭐⭐☆☆☆ Mission 1: The Profile Master

In the Guild Archive of Pyralia, you create your first **profile** (dictionary) for hero data.

**Step 1 – Create a hero profile:**
Create a dictionary `hero` with attributes such as `name`, `hero_class`, `level`, `hit_points` and suitable values, then print the dictionary using `print()`

**Step 2 – Modify and extend values:**
Change the value of `level` (e.g. to a new number) and add a new attribute `mana` with a value

**Step 3 – Monster list from profiles:**
Create a list with 3 elements; each element is a dictionary (profile) with e.g. `name`, `type`, `strength` and print the list and the values of one monster (e.g. `monster[0][\"name\"]`)

**Step 4 – Access and output:**
Access values with `hero[\"name\"]` and `hero.get(\"hero_class\")` and print them, then print a short clear summary (e.g. \"Hero X, Class Y, Level Z\")

**Bonus:** Create a nested dictionary (e.g. a hero with a sub-profile \"equipment\")."""
