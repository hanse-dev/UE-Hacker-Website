"""## ✨ Training Method 3: Naming Rules – The Rules of the Stable

**Good function names are crucial!**

**The rules:**
- **Lowercase** with underscores: `calculate_training_time`
- **Descriptive**: What the function **does** (verb)
- **Short but clear**: `get_name()` not `retrieve_horse_data_from_stable()`
- **No nouns first**: `name_get()` wrong, `get_name()` right

**Examples:**
- ✅ `calculate_training_time()`
- ✅ `create_horse_profile()`
- ✅ `check_training_level()`
- ❌ `CalculateTrainingTime()` (CamelCase)
- ❌ `calculate_the_training()` (too long)
- ❌ `training_calculate()` (noun first)"""
