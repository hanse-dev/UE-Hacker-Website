"""### Breeding Exercise 7: Calculating and Counting: __add__ and __len__ – Task 3

The class Supply with `__add__` and `__str__` is given. The list `lager = [Supply(10), Supply(20), Supply(30)]` is added up with a loop (start `total = Supply(0)`, then `total = total + v`). Print `total` (exactly Supply: 60)."""