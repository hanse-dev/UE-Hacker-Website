"""### ⭐⭐⭐⭐☆ Boss-Quest 3: Das polymorphe Tier-System

Alle Tiere können sprechen – aber jedes sagt etwas anderes!

**Schritt 1 – Basis-Klasse:**
Schreibe eine Klasse `Tier` mit Name. Füge eine Methode `laut_machen()` hinzu, die eine allgemeine Nachricht ausgibt.

**Schritt 2 – Tier-Arten:**
Leite 4 Tiere ab (z.B. Hund, Katze, Drache, Eule). Jedes überschreibt `laut_machen()` mit seinem typischen Laut.

**Schritt 3 – Tier-Parade:**
Erstelle je ein Tier und rufe für alle `laut_machen()` auf – zeige dass Python automatisch die richtige Version wählt.

**Bonus:** Füge `__str__` und `__len__` hinzu (`__len__` gibt die Länge des Namens zurück)."""
