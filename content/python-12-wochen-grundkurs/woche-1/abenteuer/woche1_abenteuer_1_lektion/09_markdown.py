"""## Zauberformel 4: Den Nutzer befragen

`input()` hält das Programm an und wartet auf eine Eingabe vom Benutzer. Die Antwort wird als **Text (String)** gespeichert.

```python
name = input(\"Wie heißt dein Held? \")
print(\"Willkommen, \" + name + \"!\")
```

> ⚠️ `input()` funktioniert nur in einer laufenden Python-Umgebung (lokal oder JupyterHub). In statischen Vorschauen bleibt es hängen."""
