"""### ⭐⭐⭐⭐⭐ Boss-Quest 2: Der Stall-Manager

Verwalte Boxen und Pferde mit Listen und einfachen Funktionen.

**Schritt 1 – Boxen anlegen:**
Erstelle `erstelle_boxen(anzahl_boxen, pferde_pro_box)`, die eine verschachtelte Liste zurückgibt (jede Box eine Liste mit Platzhaltern wie \"frei\") und gib die verschachtelte Liste zurück

**Schritt 2 – Pferde platzieren:**
Schreibe `platziere_pferd(boxen, box_index, pferd_name)`, die an der angegebenen Box-Position ein Pferd einträgt und nutze Indexzugriff und aktualisiere die Liste

**Schritt 3 – Futter verteilen:**
Erstelle `verteile_futter(boxen, totales_futter)`, die Futter gleichmäßig oder nach Belegung auf die Boxen verteilt und gib eine Liste mit Futter pro Box zurück

**Schritt 4 – Übersicht ausgeben:**
Schreibe `zeige_uebersicht(boxen)`, die alle Boxen und ihre Pferde formatiert ausgibt und gib am Ende eine kurze Zusammenfassung (Anzahl Boxen, Gesamtplätze) aus

**Bonus:** Simuliere zufällige Pflegeaufgaben (z.B. \"Stall misten\") und eine Funktion, die sie als erledigt markiert."""
