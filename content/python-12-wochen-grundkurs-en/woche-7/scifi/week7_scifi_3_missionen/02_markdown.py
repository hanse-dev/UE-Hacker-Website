"""### ⭐⭐☆☆☆ Mission 1: The Module Importer

The Nebula-7's control system won't let you into Module Central until your shuttle passes four system checks in a row – each one using a different imported module:

**Step 1 – Import modules:**
Load the modules `math`, `random` and `time` with suitable aliases (e.g. `import math as m`) and print a constant or function from each module (e.g. `math.pi`, `random.randint(1, 10)`)

**Step 2 – Wait time simulation:**
Create a small function or script that generates a random number of seconds (1–3) with `random` and uses `time.sleep()`, and print a short message before and after the wait (e.g. \"Docking manoeuvre in progress...\")

**Step 3 – Flight path calculation:**
Use `math.sin` and `math.pi` to calculate a height or value for an angle (e.g. 90° in radians) and print the result formatted – the control system uses this to verify your course

**Step 4 – Crew generator:**
Create a name list with 3–5 crew names and pick a random name with `random.choice()` to decide who leads the landing party, and print the selected name

**Bonus:** Add another function using the `datetime` module (e.g. display the current time)."""
