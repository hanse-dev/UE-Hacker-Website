"""## ✨ Data Collection 3: Searching and Sorting Lists – The Search Algorithm *(= step-by-step solution)*

**The most powerful search methods:**

**Searching and checking:**
- `index(element)` – Returns the index of the element
- `element in list_` – Checks if element is present
- `count(element)` – Counts occurrences

**Sorting:**
- `sort()` – Sorts the list itself
- `sorted(list_)` – Returns a sorted copy
- `reverse()` – Reverses the order"""
