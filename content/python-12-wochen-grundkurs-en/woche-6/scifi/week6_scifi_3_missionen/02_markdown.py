"""### ⭐⭐☆☆☆ Mission 1: The Data Collector

In the Data Central of Nebula-7, you create your first ordered data collection for ships.

**Step 1 – Create ship list:**
Create a list `ships` with 5 different ship types (e.g. \"Nebula-Explorer\", \"Star-Fighter\") and print the list with `print()`

**Step 2 – Add elements:**
Use `append()` to add three more ships to the end of the list and print the list after each step or at the end

**Step 3 – Remove an element and find a position:**
Remove a specific ship from the list using `remove(\"Name\")` and find the position of another ship using `index(\"Name\")` and print it

**Step 4 – Sort and output:**
Sort the ships alphabetically with `sort()` and print the sorted list and the number of ships with `len()`

**Bonus:** Create a nested list where each ship also has additional details (e.g. class, crew size)."""
