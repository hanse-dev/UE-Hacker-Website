# Exercise: Putting It All Together

Combine variables, lists, loops, functions, and dictionaries.

```python
def analyze(numbers):
    total = sum(numbers)
    average = total / len(numbers)
    print(f"Sum: {total}, Average: {average}")

grades = [85, 92, 78, 96, 88]
analyze(grades)
```

You're ready for the 12-week Python fundamentals course!
