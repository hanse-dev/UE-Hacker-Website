# f-Strings – Mixing Text and Variables ✨

With f-Strings you can put variables directly into text. Just write `f` before the quotation marks and `{variable}` where you want the value.

```python
animal = "Cat"
print(f"My favorite animal is a {animal}!")
# prints: My favorite animal is a Cat!
```
