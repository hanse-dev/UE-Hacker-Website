"""### ⭐⭐☆☆☆ Mission 1: Der Datensammler

Im Daten-Zentral der Nebula-7 erstellst du deine erste geordnete Datensammlung für Schiffe.

**Schritt 1 – Schiffsliste anlegen:**
Erstelle eine Liste `schiffe` mit 5 verschiedenen Schiffstypen (z.B. \"Nebula-Explorer\", \"Star-Fighter\") und gib die Liste mit `print()` aus

**Schritt 2 – Elemente ergänzen:**
Füge mit `append()` drei weitere Schiffe an das Ende der Liste an und gib die Liste nach jedem Schritt oder am Ende erneut aus

**Schritt 3 – Element entfernen und Position finden:**
Entferne mit `remove(\"Name\")` ein bestimmtes Schiff aus der Liste und finde mit `index(\"Name\")` die Position eines anderen Schiffes und gib sie aus

**Schritt 4 – Sortieren und Ausgabe:**
Sortiere die Schiffe alphabetisch mit `sort()` und gib die sortierte Liste und die Anzahl der Schiffe mit `len()` aus

**Bonus:** Erstelle eine verschachtelte Liste, in der zu jedem Schiff noch Details (z.B. Klasse, Besatzung) stehen."""
