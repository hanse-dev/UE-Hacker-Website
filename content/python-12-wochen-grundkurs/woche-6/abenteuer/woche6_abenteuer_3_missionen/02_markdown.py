"""### ⭐⭐☆☆☆ Mission 1: Der Schatzsammler

In der Schatzkammer erstellst du deine erste geordnete Schatzliste.

**Schritt 1 – Schatzliste anlegen:**
Erstelle eine Liste `schaetze` mit 5 verschiedenen Schätzen (z.B. \"Gold\", \"Kristall\", \"Amulett\") und gib die Liste mit `print()` aus und nutze eckige Klammern `[]`

**Schritt 2 – Schätze ergänzen:**
Füge mit `append()` drei weitere Schätze an und gib die Liste danach aus und optional: Nutze `insert(0, \"Name\")`, um einen Schatz an den Anfang zu setzen

**Schritt 3 – Schatz entfernen und Position finden:**
Entferne mit `remove(\"Name\")` einen Schatz und finde mit `index(\"Name\")` die Position eines anderen und gib die aktuelle Liste und die gefundene Position aus

**Schritt 4 – Sortieren und Ausgabe:**
Sortiere die Schätze mit `sort()` und gib die Liste sowie `len(schaetze)` aus

**Bonus:** Erstelle eine verschachtelte Liste mit Schatz-Details (Name, Wert, Seltenheit)."""
