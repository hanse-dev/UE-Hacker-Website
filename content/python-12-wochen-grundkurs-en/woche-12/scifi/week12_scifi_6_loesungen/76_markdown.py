"""### Extra Challenge 2: Saving the World Map – Task 1

Step 1: the world `world` (rooms with description and exits) is given. Turn it into text with `json.dumps` and back with `json.loads` and print `back == world` (True)."""