"""### ⭐⭐☆☆☆ Mission 1: Der Steckbrief-Meister

Im Gildenarchiv von Pyralia legst du deinen ersten **Steckbrief** (Dictionary) für Heldendaten an.

**Schritt 1 – Helden-Steckbrief anlegen:**
Erstelle ein Dictionary `held` mit Eigenschaften z.B. `name`, `klasse`, `level`, `lebenspunkte` und passenden Werten und gib das Dictionary mit `print()` aus

**Schritt 2 – Werte ändern und ergänzen:**
Ändere den Wert von `level` (z.B. auf eine neue Zahl) und füge eine neue Eigenschaft `mana` mit einem Wert hinzu

**Schritt 3 – Monster-Liste aus Steckbriefen:**
Erstelle eine Liste mit 3 Elementen; jedes Element ist ein Dictionary (Steckbrief) mit z.B. `name`, `typ`, `stärke` und gib die Liste und die Werte eines Monsters (z.B. `monster[0][\"name\"]`) aus

**Schritt 4 – Zugriff und Ausgabe:**
Greife mit `held[\"name\"]` und `held.get(\"klasse\")` auf Werte zu und gib sie aus und gib eine kurze übersichtliche Zusammenfassung (z.B. „Held X, Klasse Y, Level Z“) aus

**Bonus:** Erstelle ein verschachteltes Dictionary (z.B. Held mit Unter-Steckbrief „ausrüstung“)."""
