"""### ⭐⭐☆☆☆ Mission 1: A new room

There should be a secret chamber next to the stable aisle!

**Step 1 – New room:**
Add a room `"feedroom"` to the dictionary `world`. It needs a `"description"`, the `"exits"` (a way back `"south"` to the room `"aisle"`), a list `"items"` with one item (e.g. `Oatsack`) and `"enemy": None`.

**Step 2 – Connect:**
Add an exit `"north"` to the room `"aisle"` that leads to the new room.

**Step 3 – Test:**
Create a `Player` in `"aisle"`, let them go `north` and take the item.

**Bonus:** Give the room a second door that leads to `"tackroom"` (don't forget the way back!)."""