"""### ⭐⭐⭐☆☆ Mission 2: The Fleet Formation

Create a complete fleet in formation!

**Step 1 – Function for one spaceship:**
Create a function `draw_ship(x, y, size)` that draws a simple ship (e.g. triangle or trapezoid) and the ship should appear at position (x, y) with the given size

**Step 2 – Five ships in V-formation:**
Call the function 5 times with different positions so the ships are arranged in a V-shape and use different sizes (e.g. the centre ship larger)

**Step 3 – Colour scheme and connections:**
Set a uniform colour scheme (e.g. silver/cyan) and optionally draw dotted lines between the ships (with `penup()`, short dashes in a loop)

**Bonus:** Animate the formation with `turtle.ontimer()` or loops!"""
