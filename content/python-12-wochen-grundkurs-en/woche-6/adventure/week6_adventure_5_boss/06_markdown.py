"""### ⭐⭐⭐⭐☆ Boss Quest 3: The Quest Database

Manage quests in a list: create, search, filter, and evaluate them.

**Step 1 – Create a quest:**
Create `create_quest(name, goal, difficulty)` that returns a dictionary with \"name\", \"goal\", \"difficulty\" (and optionally \"status\") and return the dictionary with `return`

**Step 2 – Add quest to list:**
Create a list `quests` and write `add_quest(quest, quest_list)` that adds with `append()` and prints a confirmation

**Step 3 – Search quests:**
Write `search_quests(quest_list, search_term)` that returns all quests whose name or goal contains the search term

**Step 4 – Filter by status:**
Create `filter_by_status(quest_list, status)` and return the filtered list

**Step 5 – Print statistics:**
Write `quest_stats(quest_list)` that prints the count and optionally the distribution by difficulty; call all functions in the main section

**Bonus:** Add a \"reward\" or \"success\" field to each quest and print a success statistic."""
