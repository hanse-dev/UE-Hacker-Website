"""### Stage 7: Saving and the Finale – Task 1

The world, the player class and the player `player` (with Keycard in the inventory) are given. Build the dictionary `data` with `name`, `position`, `hp` and `inventory` (list of names) and print `json.dumps(data)` (`import json`)."""