"""### ⭐⭐☆☆☆ Mission 1: The Horse Collector

At the riding ranch you create your first organised horse list.

**Step 1 – Create a horse list:**
Create a list `horses` with 5 different horse names (e.g. \"Thunder\", \"Luna\", \"Storm\") and print the list with `print()` using square brackets `[]`

**Step 2 – Add horses:**
Add three more horses with `append()` and print the list afterwards and optionally: use `insert(0, \"Name\")` to put a horse at the beginning

**Step 3 – Remove a horse and find a position:**
Remove a horse with `remove(\"Name\")` and find the position of another with `index(\"Name\")` and print the current list and the found position

**Step 4 – Sort and output:**
Sort the horses with `sort()` and print the list and `len(horses)`

**Bonus:** Create a nested list with horse details (name, breed, age)."""
