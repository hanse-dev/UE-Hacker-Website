"""### ⭐⭐☆☆☆ Mission 1: Create Your First Robot Class

The technician needs a blueprint for all service robots on board!

**Step 1 – Create class Robot:**
Create a class with `class Robot:` and add the attributes `__init__(self, id, model, energy, function)` in the constructor *(= special method that runs automatically when an object is created)* (e.g. `self.id = id`)

**Step 2 – Create an object:**
Create an object: `r2 = Robot(2, \"Astromech\", 100, \"Repair\")` and print all properties with `print()`

**Step 3 – Second object:**
Create a second robot with different values and print its properties

**Bonus:** Add a method `status()` that returns a short status line."""
