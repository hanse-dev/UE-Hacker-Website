"""### ⭐⭐☆☆☆ Mission 1: The Spell Apprentice

In the Library of Repetitions you learn your first reusable spell formulas.

**Step 1 – Cast a greeting spell:**
Create a function `greet_wizard()` that prints two greeting lines for the wizard and call the function at least twice

**Step 2 – Mana calculation as a spell:**
Create a function `calculate_mana(level, base_mana)` that computes `level * base_mana` and return the value with `return`

**Step 3 – Check spell availability:**
Write a function `is_spell_possible(mana_cost, current_mana)` that returns `True` if there is enough mana, otherwise `False` and use the results in if-conditions and print appropriate messages

**Step 4 – Combine formulas:**
Create a small main block that greets the wizard, calculates mana and checks spell availability

**Bonus:** Create a function `generate_spell()` that randomly returns a spell (e.g. `\"Fireball\"`, `\"Healing\"`, `\"Shield\"`)."""
