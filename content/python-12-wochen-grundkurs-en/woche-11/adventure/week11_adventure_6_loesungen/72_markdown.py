"""### Extra Challenge 1: Three Levels – Task 3

Step 3: the classes are given. Write Master(Warrior) that overrides `power` and returns **twice** as much as its parent (`super().power() * 2`). Print exactly Power: 60 for Master(\"Aria\")."""