"""### ⭐⭐⭐☆☆ Mission 2: Der JSON-Konverter

Erstelle ein JSON-Konvertierungssystem!

**Schritt 1 – Pferde-Daten anlegen:**
Erstelle ein Dictionary mit Pferde-Daten (z.B. `name`, `rasse`, `alter`, `besitzer`) und gib es aus

**Schritt 2 – JSON-Export:**
Speichere das Dictionary mit `json.dump(..., indent=2)` in `pferd.json` und gib eine Bestätigung aus

**Schritt 3 – JSON-Import:**
Lade die Datei wieder mit `json.load()`, prüfe z.B. `daten[\"name\"]` und gib die geladenen Daten aus

**Schritt 4 – Kurze Statistik:**
Gib eine kurze Statistik (z.B. Anzahl, Durchschnittsalter) aus den geladenen Daten aus

**Bonus:** Baue eine einfache Validierung ein (prüfe, ob alle erwarteten Schlüssel vorhanden sind)."""
