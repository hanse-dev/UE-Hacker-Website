"""### ⭐⭐⭐⭐☆ Mission 3: Der Pferdestall

Der Stall braucht eine Struktur für alle Pferde!

**Schritt 1 – Klasse Pferd anlegen:**
Erstelle `class Pferd:` mit `__init__(self, name, rasse, geschlecht)`; weise die Attribute zu

**Schritt 2 – Methode füttern():**
Definiere `def füttern(self):` und gib darin `\"Mampf! 🐴\"` aus

**Schritt 3 – Zwei Pferde erstellen:**
Erstelle ein Islandpferd und ein Shetlandpony und rufe für beide `füttern()` auf und gib name und Rasse aus


> 💡 **Tipp:** Du entscheidest selbst, wie du das umsetzt – es gibt keinen einzig richtigen Weg!
**Bonus:** Füge eine Methode `streicheln()` hinzu, die eine Meldung ausgibt."""
