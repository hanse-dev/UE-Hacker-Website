# Variablen

Variablen speichern Werte unter einem Namen. Du kannst sie jederzeit überschreiben.

```python
username = "alex"
score = 1500
print(username)
print(score)
```

Variablennamen: Kleinbuchstaben, Unterstriche für Leerzeichen (`mein_name`). Keine Zahlen am Anfang.
