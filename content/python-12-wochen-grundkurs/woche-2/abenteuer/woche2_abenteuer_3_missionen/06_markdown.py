"""### ⭐⭐⭐⭐☆ Mission 3: Das Orakel der vier Elemente

Die letzte Prüfung: Befrage das Orakel nach den vier Elementen eines Suchenden und stelle eine Prophezeiung.

**Schritt 1 – 🔥 Feuer befragen:**
Frage mit `input()` nach dem Namen des Suchenden (Text)

**Schritt 2 – 🪨 Erde befragen:**
Frage mit `input()` nach dem Alter und wandle die Antwort mit `int()` um

**Schritt 3 – 💧 Wasser befragen:**
Frage mit `input()` nach einem Zauberkraft-Wert (z.B. \"7.5\") und wandle die Antwort mit `float()` um

**Schritt 4 – 💨 Luft bestimmen:**
Berechne einen Wahrheitswert daraus, ob die Zauberkraft über `5.0` liegt (Vergleich `>`), und gib eine Prophezeiung aus, die alle vier Antworten zusammenfasst

**Bonus:** Gib zusätzlich mit `type()` das Element (den Typ) jeder der vier Antworten aus."""
