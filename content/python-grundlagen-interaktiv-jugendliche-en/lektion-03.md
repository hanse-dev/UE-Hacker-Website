# Numbers and Arithmetic

Python supports all common arithmetic operations. `//` is integer division, `%` is the remainder (modulo), `**` is exponentiation.

```python
price = 29.99
discount = 0.1
final_price = price * (1 - discount)
print(final_price)
```
