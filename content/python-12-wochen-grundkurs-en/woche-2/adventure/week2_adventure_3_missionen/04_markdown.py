"""### ⭐⭐⭐☆☆ Mission 2: The Elemental Fusion

In the fusion chamber you test which elements can be fused directly - and which need a conversion.

**Step 1 – Fusing two 🔥 Fire Stones:**
Create two text variables (e.g. first and last name) and combine them into a name with an f-string

**Step 2 – Fusing two 🪨 Earth Stones:**
Create two whole-number variables (e.g. two gold amounts) and add them together

**Step 3 – Fusing two 💧 Water Stones:**
Create two decimal-number variables (e.g. two damage values) and calculate their average

**Step 4 – Fusing 🔥 Fire and 🪨 Earth:**
Create a text variable and a number variable and combine them with `str()` into a message (e.g. \"Level: \" + str(level))

**Bonus:** Show what happens when you try step 4 *without* `str()` - note the error message as a comment."""
