"""### Stage 2: Moving Around – Task 1

The world and `describe()` are given. Write `go(position, direction)`: if the exit exists return the new room, otherwise the old position (with a 🚫 message). Call `go(\"yard\", \"north\")` and print exactly Position: aisle."""