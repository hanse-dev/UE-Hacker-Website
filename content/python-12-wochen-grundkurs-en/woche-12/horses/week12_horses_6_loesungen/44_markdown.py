"""### Stage 7: Saving and the Finale – Task 4

Write a game loop `play(player, commands, goal_item)`: it runs every command with `execute()`. As soon as `has_item(player, goal_item)` is true it prints 🏆 Goal reached! and ends. Play the commands `go north`, `go west`, `take Broom` with the goal Broom."""