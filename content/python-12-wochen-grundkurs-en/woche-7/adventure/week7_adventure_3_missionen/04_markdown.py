"""### ⭐⭐⭐☆☆ Mission 2: The Oracle Chamber

Beyond the first gate lies the Oracle Chamber. The Dice Oracle decides which path you take through the archive.

**Step 1 – Consult the dice oracle:**
Roll 3 times with `random.randint(1, 6)` to determine your path, and print all three rolls

**Step 2 – Choose a shelf:**
Create a list of 5 shelf names and pick a random one with `random.choice()`

**Step 3 – Draw scrolls:**
Create a list of 6 scroll titles and draw 3 random, distinct scrolls from it with `random.sample()`

**Step 4 – Shuffle the reading order:**
Shuffle the drawn scrolls with `random.shuffle()` and print the random reading order

**Bonus:** Calculate the average of your three dice rolls from step 1 and print it rounded."""
