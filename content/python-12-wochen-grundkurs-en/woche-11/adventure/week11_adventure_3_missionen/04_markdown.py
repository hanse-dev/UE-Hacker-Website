"""### ⭐⭐⭐☆☆ Mission 2: The Polymorphic Arsenal

The weapon forge needs a system for different weapon types!

**Step 1 – Create three weapon classes:**
Create `class Sword:`, `class Bow:`, `class Staff:` each with `__init__(self, name)` and a method `use(self)` and each `use()` prints a different action (e.g. \"Sword hits!\", \"Arrow flies!\", \"Spell fires!\")

**Step 2 – Polymorphic function:**
Create a function `use_weapon(weapon)` that calls `weapon.use()` and the function should work with all three weapon types

**Step 3 – Test all weapons:**
Create one Sword, one Bow, and one Staff object and call `use_weapon()` for each weapon

**Bonus:** Create a weapon factory function that produces the matching weapon based on type."""
