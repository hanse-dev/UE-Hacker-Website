"""### ⭐⭐☆☆☆ Mission 1: The Tool Importer

In the workshop of Sunny Valley Ranch you learn how to load toolboxes (modules) and work with aliases.

**Step 1 – Import toolboxes:**
Load the modules `math`, `random` and `time` with matching aliases (e.g. `import math as m`) and print one constant or function from each module (e.g. `math.pi`, `random.randint(1, 10)`)

**Step 2 – Wait time simulation:**
Create a small function or script that generates a random number of seconds (1–3) with `random` and uses `time.sleep()`, and print a short message before and after the wait time (e.g. \"Waiting for horse …\")

**Step 3 – Jump calculation:**
Use `math.sin` and `math.pi` to calculate a jump height or value for an angle (e.g. 90° in radians) and print the result in a formatted way

**Step 4 – Rider generator:**
Create a list of names with 3–5 rider names and pick a random name with `random.choice()` and print the selected name

**Bonus:** Add another function using the `datetime` module (e.g. show the current time)."""
