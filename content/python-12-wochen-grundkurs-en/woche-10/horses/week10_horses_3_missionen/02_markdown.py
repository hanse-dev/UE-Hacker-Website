"""### ⭐⭐☆☆☆ Mission 1: Create Your First Horse Class

The stable master needs an overview of all horses on the farm!

**Step 1 – Create class Horse:**
Create a class with `class Horse:` and add the attributes `__init__(self, name, breed, age, gender)` in the constructor *(= special method that runs automatically when an object is created)* (e.g. `self.name = name`)

**Step 2 – Create an object:**
Create an object: `lucky = Horse(\"Lucky\", \"Icelandic\", 8, \"Gelding\")` and print all properties with `print()`

**Step 3 – Second horse:**
Create a second horse with different values and print its properties

**Bonus:** Add a method `introduce()` that returns a short description of the horse."""
