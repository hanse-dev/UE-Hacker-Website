"""### ⭐⭐⭐☆☆ Mission 2: The JSON Converter

Create a JSON conversion system!

**Step 1 – Create horse data:**
Create a dictionary with horse data (e.g. `name`, `breed`, `age`, `owner`) and print it

**Step 2 – JSON export:**
Save the dictionary with `json.dump(..., indent=2)` to `horse.json` and print a confirmation

**Step 3 – JSON import:**
Load the file again with `json.load()`, check e.g. `data[\"name\"]`, and print the loaded data

**Step 4 – Short statistics:**
Print a short summary (e.g. count, average age) from the loaded data

**Bonus:** Add simple validation (check that all expected keys are present)."""
