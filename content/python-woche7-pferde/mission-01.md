# ⭐⭐☆☆☆ Mission 1: Die drei Kisten

Im Stall stehen drei Werkzeugkisten (Module). Öffne sie, benutze ihr Werkzeug und beweise, dass es funktioniert.

**Bonus (freiwillig, ohne Prüfung):** Füge mit from datetime import datetime einen Zeitstempel hinzu, wann du fertig warst.
