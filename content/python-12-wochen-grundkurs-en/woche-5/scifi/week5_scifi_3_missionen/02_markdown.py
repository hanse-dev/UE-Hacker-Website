"""### ⭐⭐☆☆☆ Mission 1: The System Technician

At Nebula-7 HQ you create your first system protocols.

**Step 1 – Program a greeting function:**
Create a function `greet_crew()` that prints two greeting lines and call the function at least twice

**Step 2 – Energy calculation as a function:**
Create a function `calculate_energy(level, duration)` that computes `level * duration * 10` and return the result with `return`

**Step 3 – Check system status:**
Create a function `is_system_ready(energy)` that returns `True` if `energy >= 100`, otherwise `False` and use the results in if-conditions and print appropriate status messages

**Step 4 – Combine protocols:**
Call all three functions in a small main program and use the calculated energy to check the system status

**Bonus:** Create a function `generate_system_event()` that randomly returns a system event like `\"Solar Storm\"`, `\"Sensor Error\"` or `\"All Stable\"`."""
