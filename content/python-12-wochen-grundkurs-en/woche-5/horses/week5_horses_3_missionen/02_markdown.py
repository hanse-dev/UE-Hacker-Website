"""### ⭐⭐☆☆☆ Mission 1: The Training Beginner

In the Training Hall of Perfection you create your first reusable training routines for horses.

**Step 1 – Create a greeting routine:**
Create a function `greet_horse()` that prints two greeting lines for the horse and call the function at least twice

**Step 2 – Calculate training intensity:**
Create a function `calculate_intensity(level, duration)` that computes `level * duration * 5` and return the result with `return`

**Step 3 – Check training possibility:**
Create a function `is_training_possible(energy)` that returns `True` if `energy >= 50`, otherwise `False` and use the results in if-conditions and print appropriate messages

**Step 4 – Combine routines:**
Create a small main block that greets the horse, calculates intensity and checks training possibility

**Bonus:** Create a function `generate_training()` that randomly returns a training type (e.g. `\"Dressage\"`, `\"Jumping\"`, `\"Cross-Country\"`)."""
