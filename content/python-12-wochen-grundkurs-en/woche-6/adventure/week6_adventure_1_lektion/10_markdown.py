"""## Collection Spell 3: Searching and Sorting Lists

**Searching and checking:**
- `index(element)` – returns the position of the element
- `element in list_` – checks if element is present (`True`/`False`)
- `count(element)` – counts how often an element appears
- `len(list_)` – returns the number of elements

**Sorting:**
- `sort()` – sorts the list (modifies it directly)
- `sorted(list_)` – returns a sorted copy
- `reverse()` – reverses the order

**Index and value at the same time:**
- `enumerate(list_)` – provides both **index and value** as a pair in a `for` loop:

```python
for index, element in enumerate(list_):
    print(f\"Position {index}: {element}\")
```"""
