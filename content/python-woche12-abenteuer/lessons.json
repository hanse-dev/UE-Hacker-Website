[
  {
    "id": "lektion-01",
    "section": "lektion",
    "title": "Etappe 1: Die Karte der Welt",
    "file": "lektion-01.md",
    "lessonSummary": "Stark! Deine Welt steht als verschachteltes Dictionary.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: die Welt und `beschreibe()`.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\nbeschreibe(\"eingang\")\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "Ausgänge: norden"
        }
      },
      {
        "instruction": "Die Welt `welt` ist vorgegeben. Gib die Ausgänge des Raums halle aus, verbunden mit `\", \".join(...)` – genau sueden, osten, westen.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "sueden, osten, westen"
        }
      },
      {
        "instruction": "Das Dictionary `welt` ist vorgegeben. Schreibe `beschreibe(room_name)`, die `📍 Raumname: Beschreibung` ausgibt (`room_name.capitalize()`, dann `: ` und die Beschreibung). Rufe sie für eingang auf.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "📍 Eingang: Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch."
        }
      },
      {
        "instruction": "Die Welt ist vorgegeben. Füge im Raum halle einen weiteren Ausgang unten nach keller hinzu (`welt[\"halle\"][\"ausgaenge\"][\"unten\"] = \"keller\"`) und gib genau Ausgänge: 4 aus (Anzahl der Ausgänge).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Ausgänge: 4"
        }
      }
    ]
  },
  {
    "id": "lektion-02",
    "section": "lektion",
    "title": "Etappe 2: Sich bewegen",
    "file": "lektion-02.md",
    "lessonSummary": "Prima! Du bewegst dich durch die Welt.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: eine Befehlsliste abspielen.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nposition = \"eingang\"\nfor command in [\"norden\", \"osten\", \"norden\", \"westen\", \"westen\"]:\n    print(f\"\\n> {command}\")\n    position = gehe(position, command)\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "🚫 Dort geht es nicht weiter!"
        }
      },
      {
        "instruction": "Die Welt und `beschreibe()` sind vorgegeben. Schreibe `gehe(position, richtung)`: Gibt es den Ausgang, gib den neuen Raum zurück, sonst die alte Position (mit 🚫-Meldung). Rufe `gehe(\"eingang\", \"norden\")` auf und gib genau Position: halle aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Position: halle"
        }
      },
      {
        "instruction": "Welt und `gehe()` sind vorgegeben. Gehe mit einer `for`-Schleife die Befehle norden und osten ab (Start eingang) und gib genau Position: schatzkammer aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Position: schatzkammer"
        }
      },
      {
        "instruction": "Welt und `gehe()` sind vorgegeben. Versuche von eingang aus nach westen zu gehen (dort ist keine Tür) und gib genau Position: eingang aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Position: eingang"
        }
      }
    ]
  },
  {
    "id": "lektion-03",
    "section": "lektion",
    "title": "Etappe 3: Gegenstände",
    "file": "lektion-03.md",
    "lessonSummary": "Klasse! Deine Räume enthalten jetzt Objekte.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: Gegenstände in Räumen.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nfirst_room = \"eingang\"\nfirst_item = welt[first_room][\"gegenstaende\"][0]\nprint(f\"Im {first_room}: {first_item.name} – {first_item.beschreibung}\")\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "Im eingang: Fackel – Sie leuchtet in dunklen Ecken."
        }
      },
      {
        "instruction": "Schreibe die Klasse `Gegenstand` mit `__init__(self, name, beschreibung)`. Erzeuge `Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")` und gib den Namen aus (genau Fackel).",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Fackel"
        }
      },
      {
        "instruction": "Welt und Klasse `Gegenstand` sind vorgegeben. Lege im Raum quelle die Liste `[\"gegenstaende\"]` mit `Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")` an und gib die Namen mit einer List Comprehension aus – genau ['Schwert'].",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "['Schwert']"
        }
      },
      {
        "instruction": "Welt und Gegenstände sind vorgegeben. Zähle mit einer Schleife über `welt.values()`, wie viele Gegenstände es in allen Räumen zusammen gibt, und gib genau Anzahl: 3 aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Anzahl: 3"
        }
      }
    ]
  },
  {
    "id": "lektion-04",
    "section": "lektion",
    "title": "Etappe 4: Spieler und Inventar",
    "file": "lektion-04.md",
    "lessonSummary": "Super! Dein Spieler trägt Gegenstände bei sich.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: der Spieler sammelt Gegenstände ein.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\nspieler.umschauen()\nspieler.nimm(\"Fackel\")\nspieler.gehe(\"norden\")\nspieler.gehe(\"westen\")\nspieler.nimm(\"Schwert\")\nspieler.zeige_inventar()\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "🎒 Im Beutel: Fackel, Schwert"
        }
      },
      {
        "instruction": "Welt, Gegenstände und Klasse `Spieler` sind vorgegeben. Erzeuge `spieler = Spieler(\"Mira\", \"eingang\")`, nimm Fackel und gib genau Anzahl: 1 aus (Länge des Inventars).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Anzahl: 1"
        }
      },
      {
        "instruction": "Alles ist vorgegeben. Erzeuge den Spieler in eingang, nimm Fackel, gehe norden und westen, nimm Schwert und zeige das Inventar an (`zeige_inventar()`).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "🎒 Im Beutel: Fackel, Schwert"
        }
      },
      {
        "instruction": "Alles ist vorgegeben und der Spieler `spieler` hat schon Fackel eingesteckt. Schreibe `hat_gegenstand(spieler, name)`, die True zurückgibt, wenn ein Gegenstand mit diesem Namen im Inventar liegt (sonst False). Gib das Ergebnis für Fackel und für Schwert aus (zwei Zeilen).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\nspieler.nimm(\"Fackel\")\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "True\nFalse"
        }
      }
    ]
  },
  {
    "id": "lektion-05",
    "section": "lektion",
    "title": "Etappe 5: Gegner und Kampf",
    "file": "lektion-05.md",
    "lessonSummary": "Stark! Dein Spiel hat einen Gegner und einen Kampf.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: ein Übungs-Kampf.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\nspieler = Spieler(\"Testheld\", \"schatzkammer\")\nkampf(spieler, Gegner(\"Übungs-Drache\", 1, 6))\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "⚔️ Kampf: Testheld gegen Übungs-Drache!"
        }
      },
      {
        "instruction": "Schreibe die Klasse `Gegner` mit `__init__(self, name, hp, staerke)` und der Methode `ist_besiegt(self)` (True, wenn `hp <= 0`). Erzeuge `Gegner(\"Drache\", 10, 3)`, gib `ist_besiegt()` aus, setze `hp = 0` und gib es noch einmal aus.",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "False\nTrue"
        }
      },
      {
        "instruction": "Alles inklusive `kampf()` ist vorgegeben. Lasse `Spieler(\"Mira\", \"schatzkammer\")` gegen `Gegner(\"Drache\", 1, 6)` kämpfen (1 Lebenspunkt – der erste Treffer gewinnt) und gib genau Sieg: True aus (Rückgabewert von `kampf`).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Sieg: True"
        }
      },
      {
        "instruction": "Alles ist vorgegeben. Kämpfe wie eben, merke dir den Gegner in `foe` und gib danach genau Besiegt: True aus (`foe.ist_besiegt()`).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Besiegt: True"
        }
      }
    ]
  },
  {
    "id": "lektion-06",
    "section": "lektion",
    "title": "Etappe 6: Falsche Eingaben abfangen",
    "file": "lektion-06.md",
    "lessonSummary": "Klasse! Dein Spiel stürzt bei falschen Eingaben nicht mehr ab.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: falsche Befehle.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\nfuehre_aus(spieler, \"hallo\")\nfuehre_aus(spieler, \"tanze wild\")\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'."
        }
      },
      {
        "instruction": "Zerlege den Text `\"hallo\"` mit `action, target = \"hallo\".split()` in einem `try`-Block. Fange den `ValueError` ab und gib genau Ungültiger Befehl aus.",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Ungültiger Befehl"
        }
      },
      {
        "instruction": "Alles inklusive `fuehre_aus()` ist vorgegeben. Erzeuge den Spieler und führe die Befehle `gehe norden` und `tanze wild` mit einer Schleife aus. Die zweite Meldung beginnt mit 🤔 'tanze'.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "🤔 'tanze' kenne ich nicht. Versuche 'gehe' oder 'nimm'."
        }
      },
      {
        "instruction": "Schreibe `parse(text)`: Sie zerlegt den Text mit `split()` in `(action, target)` und gibt beides als Tupel zurück – bei einem `ValueError` gibt sie `None` zurück. Gib `parse(\"gehe norden\")` und `parse(\"hallo\")` aus (zwei Zeilen).",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "('gehe', 'norden')\nNone"
        }
      }
    ]
  },
  {
    "id": "lektion-07",
    "section": "lektion",
    "title": "Etappe 7: Speichern und das Finale",
    "file": "lektion-07.md",
    "lessonSummary": "Geschafft! Dein Spiel speichert, lädt und läuft in einer Spielschleife.",
    "tasks": [
      {
        "instruction": "Führe dieses Beispiel aus: speichern, laden und das Finale.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\nimport json\n\ndef speichern(spieler, filename=\"stand.json\"):\n    data = {\n        \"name\": spieler.name,\n        \"position\": spieler.position,\n        \"hp\": spieler.hp,\n        \"inventar\": [{\"name\": g.name, \"beschreibung\": g.beschreibung} for g in spieler.inventar],\n    }\n    with open(filename, \"w\", encoding=\"utf-8\") as f:\n        json.dump(data, f, ensure_ascii=False, indent=2)\n    print(f\"💾 Spielstand von {spieler.name} gespeichert.\")\n\ndef laden(filename=\"stand.json\"):\n    try:\n        with open(filename, \"r\", encoding=\"utf-8\") as f:\n            data = json.load(f)\n    except FileNotFoundError:\n        print(\"📂 Es gibt noch keinen Spielstand.\")\n        return None\n    spieler = Spieler(data[\"name\"], data[\"position\"])\n    spieler.hp = data[\"hp\"]\n    for entry in data[\"inventar\"]:\n        spieler.inventar.append(Gegenstand(entry[\"name\"], entry[\"beschreibung\"]))\n    print(f\"📂 Spielstand von {spieler.name} geladen.\")\n    return spieler\n\ndef spiele(spieler, commands, goal_item=\"Schatz\"):\n    for text in commands:\n        print(f\"\\n> {text}\")\n        fuehre_aus(spieler, text)\n        if spieler.hp <= 0:\n            print(\"💀 Game Over – zu stark. Versuche es noch einmal!\")\n            return\n        if hat_gegenstand(spieler, goal_item):\n            print(\"🏆 Du hast den Schatz von Pyralia gefunden. Die Gilde feiert dich!\")\n            return\n\nspieler = Spieler(\"Mira\", \"eingang\")\nspieler.nimm(\"Fackel\")\nspieler.gehe(\"norden\")\nspieler.gehe(\"westen\")\nspieler.nimm(\"Schwert\")\nspeichern(spieler)\nloaded = laden()\nloaded.zeige_inventar()\nspiele(loaded, [\"gehe osten\", \"gehe osten\", \"nimm Schatz\"])\n",
        "example": true,
        "validation": {
          "type": "output_contains",
          "expected": "💾 Spielstand von Mira gespeichert."
        }
      },
      {
        "instruction": "Welt, Spieler-Klasse und der Spieler `spieler` (mit Fackel im Inventar) sind vorgegeben. Baue das Dictionary `data` mit `name`, `position`, `hp` und `inventar` (Liste der Namen) und gib `json.dumps(data)` aus (`import json`).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\nspieler.nimm(\"Fackel\")\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "{\"name\": \"Mira\", \"position\": \"eingang\", \"hp\": 20, \"inventar\": [\"Fackel\"]}"
        }
      },
      {
        "instruction": "Schreibe `laden(filename)`, die die Datei mit `json.load` liest. Fehlt die Datei, fange den `FileNotFoundError` ab, gib genau 📂 Es gibt noch keinen Spielstand. aus und gib `None` zurück. Rufe `laden(\"gibtsnicht.json\")` auf.",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "📂 Es gibt noch keinen Spielstand."
        }
      },
      {
        "instruction": "Alle Bausteine sind vorgegeben. Erzeuge den Spieler, nimm Fackel, speichere ihn mit `speichern(spieler, \"test.json\")`, lade ihn mit `laden(\"test.json\")` und gib genau Position: eingang aus (Position des geladenen Spielers).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\nimport json\n\ndef speichern(spieler, filename=\"stand.json\"):\n    data = {\n        \"name\": spieler.name,\n        \"position\": spieler.position,\n        \"hp\": spieler.hp,\n        \"inventar\": [{\"name\": g.name, \"beschreibung\": g.beschreibung} for g in spieler.inventar],\n    }\n    with open(filename, \"w\", encoding=\"utf-8\") as f:\n        json.dump(data, f, ensure_ascii=False, indent=2)\n    print(f\"💾 Spielstand von {spieler.name} gespeichert.\")\n\ndef laden(filename=\"stand.json\"):\n    try:\n        with open(filename, \"r\", encoding=\"utf-8\") as f:\n            data = json.load(f)\n    except FileNotFoundError:\n        print(\"📂 Es gibt noch keinen Spielstand.\")\n        return None\n    spieler = Spieler(data[\"name\"], data[\"position\"])\n    spieler.hp = data[\"hp\"]\n    for entry in data[\"inventar\"]:\n        spieler.inventar.append(Gegenstand(entry[\"name\"], entry[\"beschreibung\"]))\n    print(f\"📂 Spielstand von {spieler.name} geladen.\")\n    return spieler\n\ndef spiele(spieler, commands, goal_item=\"Schatz\"):\n    for text in commands:\n        print(f\"\\n> {text}\")\n        fuehre_aus(spieler, text)\n        if spieler.hp <= 0:\n            print(\"💀 Game Over – zu stark. Versuche es noch einmal!\")\n            return\n        if hat_gegenstand(spieler, goal_item):\n            print(\"🏆 Du hast den Schatz von Pyralia gefunden. Die Gilde feiert dich!\")\n            return\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Position: eingang"
        }
      },
      {
        "instruction": "Schreibe eine Spielschleife `spiele(spieler, commands, goal_item)`: Sie führt jeden Befehl mit `fuehre_aus()` aus. Sobald `hat_gegenstand(spieler, goal_item)` stimmt, gibt sie 🏆 Ziel erreicht! aus und endet. Spiele die Befehle `gehe norden`, `gehe westen`, `nimm Schwert` mit dem Ziel Schwert.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "🏆 Ziel erreicht!"
        }
      }
    ]
  },
  {
    "id": "debug-01",
    "section": "debug",
    "title": "Debug-Quest",
    "file": "debug-01.md",
    "lessonSummary": "Alle Bugs gefunden! Dictionary-Zugriff (Woche 8), `self.` bei Attributen (Woche 10) und der Datei-Modus (Woche 9) sind typische Fehlerquellen.",
    "tasks": [
      {
        "instruction": "🐛 Bug #1 – Ziel: Das Programm soll Position: halle ausgeben. Was ist falsch?",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    raum = welt[position]\n    if richtung in raum:\n        return raum[\"ausgaenge\"][richtung]\n    return position\n\nposition = gehe(\"eingang\", \"norden\")\nprint(f\"Position: {position}\")\n",
        "validation": {
          "type": "output_contains",
          "expected": "Position: halle"
        }
      },
      {
        "instruction": "🐛 Bug #2 – Ziel: Das Programm soll Anzahl: 1 ausgeben. Was ist falsch?",
        "codeTemplate": "class Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        inventar = []\n\n    def nimm(self, item_name):\n        self.inventar.append(item_name)\n\nspieler = Spieler(\"Mira\", \"eingang\")\nspieler.nimm(\"Fackel\")\nprint(f\"Anzahl: {len(spieler.inventar)}\")\n",
        "validation": {
          "type": "output_contains",
          "expected": "Anzahl: 1"
        }
      },
      {
        "instruction": "🐛 Bug #3 – Ziel: Das Programm soll 💾 Spielstand von Mira gespeichert. ausgeben. Was ist falsch?",
        "codeTemplate": "import json\ndata = {\"name\": \"Mira\", \"hp\": 20}\nwith open(\"stand.json\", \"r\") as f:\n    json.dump(data, f)\nprint(\"💾 Spielstand von Mira gespeichert.\")\n",
        "validation": {
          "type": "output_contains",
          "expected": "💾 Spielstand von Mira gespeichert."
        }
      }
    ]
  },
  {
    "id": "mission-01",
    "section": "mission",
    "title": "Mission 1: Ein neuer Raum",
    "file": "mission-01.md",
    "lessonSummary": "Mission geschafft: Deine Welt ist größer geworden!",
    "tasks": [
      {
        "instruction": "Schritt 1 – Raum anlegen: Welt und `beschreibe()` sind vorgegeben. Lege `welt[\"keller\"]` mit der Beschreibung Ein kühler Keller voller alter Fässer. und dem Ausgang oben nach halle an und füge in halle den Ausgang unten nach keller hinzu. Rufe `beschreibe(\"halle\")` auf.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Ausgänge: sueden, osten, westen, unten"
        }
      },
      {
        "instruction": "Schritt 2 – Gegenstand: Welt (mit Raum keller) und `Gegenstand` sind vorgegeben. Lege in keller den Gegenstand `Gegenstand(\"Heiltrank\", \"Ein roter Trank, der Wunden heilt.\")` in die Liste `\"gegenstaende\"` und gib die Namen als Liste aus (genau ['Heiltrank']).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"keller\"] = {\"beschreibung\": \"Ein kühler Keller voller alter Fässer.\", \"ausgaenge\": {\"oben\": \"halle\"}, \"gegenstaende\": [], \"gegner\": None}\nwelt[\"halle\"][\"ausgaenge\"][\"unten\"] = \"keller\"\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "['Heiltrank']"
        }
      },
      {
        "instruction": "Schritt 3 – Einsammeln: Alles ist vorgegeben, der Gegenstand Heiltrank liegt in keller. Erzeuge den Spieler in halle, gehe unten und nimm Heiltrank.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nwelt[\"keller\"] = {\"beschreibung\": \"Ein kühler Keller voller alter Fässer.\", \"ausgaenge\": {\"oben\": \"halle\"}, \"gegenstaende\": [], \"gegner\": None}\nwelt[\"halle\"][\"ausgaenge\"][\"unten\"] = \"keller\"\nwelt[\"keller\"][\"gegenstaende\"].append(Gegenstand(\"Heiltrank\", \"Ein roter Trank, der Wunden heilt.\"))\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "🎒 Du nimmst: Heiltrank"
        }
      }
    ]
  },
  {
    "id": "mission-02",
    "section": "mission",
    "title": "Mission 2: Heilung",
    "file": "mission-02.md",
    "lessonSummary": "Mission geschafft: Dein Spieler kann sich heilen!",
    "tasks": [
      {
        "instruction": "Schritt 1 – Heilen: Der Spieler `spieler` hat 8 HP. Schreibe `heile(spieler, amount)`, die `amount` zu den HP addiert, aber nie über 20. Rufe `heile(spieler, 5)` auf und gib genau HP: 13 aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\nspieler.hp = 8\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "HP: 13"
        }
      },
      {
        "instruction": "Schritt 2 – Obergrenze: `heile()` ist vorgegeben, der Spieler hat 18 HP. Heile um 5 und gib genau HP: 20 aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\ndef heile(spieler, amount):\n    spieler.hp += amount\n    if spieler.hp > 20:\n        spieler.hp = 20\n\nspieler.hp = 18\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "HP: 20"
        }
      },
      {
        "instruction": "Schritt 3 – Gegenstand benutzen: `heile()` ist vorgegeben, der Spieler hat 5 HP und Heiltrank im Inventar. Schreibe `benutze(spieler)`: Sie sucht Heiltrank im Inventar, entfernt ihn und heilt um 10 – sonst gibt sie Du hast nichts zum Heilen. aus. Rufe sie zweimal auf und gib dazwischen genau HP: 15 aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\ndef heile(spieler, amount):\n    spieler.hp += amount\n    if spieler.hp > 20:\n        spieler.hp = 20\n\nspieler.hp = 5\nspieler.inventar.append(Gegenstand(\"Heiltrank\", \"Ein roter Trank, der Wunden heilt.\"))\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "HP: 15\nDu hast nichts zum Heilen."
        }
      }
    ]
  },
  {
    "id": "mission-03",
    "section": "mission",
    "title": "Mission 3: Neue Befehle",
    "file": "mission-03.md",
    "lessonSummary": "Mission geschafft: Dein Spiel versteht neue Befehle!",
    "tasks": [
      {
        "instruction": "Schritt 1 – inventar: Alles inklusive `fuehre_aus()` ist vorgegeben. Schreibe `fuehre_aus2(spieler, text)`: Ist `text` gleich inventar, ruft sie `zeige_inventar()` auf, sonst `fuehre_aus()`. Führe `nimm Fackel` und dann inventar aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "🎒 Im Beutel: Fackel"
        }
      },
      {
        "instruction": "Schritt 2 – hp: Alles ist vorgegeben. Schreibe `fuehre_aus2(spieler, text)`: Ist `text` gleich hp, gibt sie ❤️ HP: gefolgt von den HP aus, sonst ruft sie `fuehre_aus()` auf. Führe hp aus (genau ❤️ HP: 20).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\ndef fuehre_aus(spieler, text):\n    try:\n        action, target = text.split()\n    except ValueError:\n        print(\"🤔 Ich verstehe nur Befehle aus zwei Wörtern, z.B. 'gehe norden' oder 'nimm Fackel'.\")\n        return\n    if action == \"gehe\":\n        spieler.gehe(target)\n        pruefe_gegner(spieler)\n    elif action == \"nimm\":\n        spieler.nimm(target)\n    else:\n        print(f\"🤔 '{action}' kenne ich nicht. Versuche 'gehe' oder 'nimm'.\")\n\nspieler = Spieler(\"Mira\", \"eingang\")\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "❤️ HP: 20"
        }
      },
      {
        "instruction": "Schritt 3 – Hilfe: Schreibe `hilfe()`, die die Befehle in einer Zeile ausgibt: Befehle: gehe, nimm, inventar, hp (mit `\", \".join([...])`).",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Befehle: gehe, nimm, inventar, hp"
        }
      }
    ]
  },
  {
    "id": "boss-01",
    "section": "boss",
    "title": "Extra-Herausforderung 1: Ein zweiter Gegner",
    "file": "boss-01.md",
    "lessonSummary": "Geschafft: Du steuerst Kämpfe und ihre Ausgänge!",
    "tasks": [
      {
        "instruction": "Schritt 1: Alles ist vorgegeben. Setze in halle den Gegner `Gegner(\"Wache\", 1, 1)` (`welt[\"halle\"][\"gegner\"]`), erzeuge den Spieler in halle und rufe `pruefe_gegner(spieler)` auf.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "🎉 Wache wurde besiegt!"
        }
      },
      {
        "instruction": "Schritt 2: Alles ist vorgegeben. Wie Schritt 1, aber gib danach genau Besiegt: True aus (`welt[\"halle\"][\"gegner\"].ist_besiegt()`).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Besiegt: True"
        }
      },
      {
        "instruction": "Schritt 3 – Übermächtiger Gegner: Alles ist vorgegeben. Lasse den Spieler gegen `Gegner(\"Drache\", 1000, 30)` kämpfen (er kann nicht gewinnen) und gib genau Sieg: False aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Sieg: False"
        }
      }
    ]
  },
  {
    "id": "boss-02",
    "section": "boss",
    "title": "Extra-Herausforderung 2: Die Weltkarte speichern",
    "file": "boss-02.md",
    "lessonSummary": "Geschafft: Deine Weltkarte übersteht jedes Speichern!",
    "tasks": [
      {
        "instruction": "Schritt 1: Die Welt `welt` (Räume mit Beschreibung und Ausgängen) ist vorgegeben. Wandle sie mit `json.dumps` in Text und mit `json.loads` zurück und gib `back == welt` aus (True).",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "True"
        }
      },
      {
        "instruction": "Schritt 2: Die Welt ist vorgegeben. Schreibe sie mit `json.dump` in die Datei welt.json, lies sie mit `json.load` wieder ein und gib genau Räume: 4 aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Räume: 4"
        }
      },
      {
        "instruction": "Schritt 3: Die Welt ist vorgegeben. Gib die Raumnamen alphabetisch sortiert als Liste aus: `print(sorted(welt))`.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "['eingang', 'halle', 'quelle', 'schatzkammer']"
        }
      }
    ]
  },
  {
    "id": "boss-03",
    "section": "boss",
    "title": "Extra-Herausforderung 3: Die Bestenliste",
    "file": "boss-03.md",
    "lessonSummary": "Geschafft: Deine Bestenliste sortiert und speichert!",
    "tasks": [
      {
        "instruction": "Schritt 1: Das Dictionary `bestenliste = {\"Mira\": 120, \"Tom\": 90, \"Lena\": 150}` ist vorgegeben. Finde mit einer Schleife den Namen mit den meisten Punkten und gib genau Bester: Lena aus.",
        "codeTemplate": "bestenliste = {\"Mira\": 120, \"Tom\": 90, \"Lena\": 150}\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Bester: Lena"
        }
      },
      {
        "instruction": "Schritt 2: Das Dictionary ist vorgegeben. Sortiere die Namen mit `sorted(bestenliste, key=bestenliste.get, reverse=True)` und gib pro Name eine Zeile wie Lena: 150 aus.",
        "codeTemplate": "bestenliste = {\"Mira\": 120, \"Tom\": 90, \"Lena\": 150}\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Lena: 150\nMira: 120\nTom: 90"
        }
      },
      {
        "instruction": "Schritt 3: Das Dictionary ist vorgegeben. Speichere es mit `json.dump` in `scores.json`, lade es zurück und gib genau Anzahl: 3 aus (Anzahl der Einträge).",
        "codeTemplate": "bestenliste = {\"Mira\": 120, \"Tom\": 90, \"Lena\": 150}\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Anzahl: 3"
        }
      }
    ]
  },
  {
    "id": "boss-04",
    "section": "boss",
    "title": "Extra-Herausforderung 4: Das ganze Spiel",
    "file": "boss-04.md",
    "lessonSummary": "Geschafft: Du hast alle Wochen des Kurses angewendet!",
    "tasks": [
      {
        "instruction": "Schritt 1 – Zufall (Woche 7): Schreibe `wuerfle()`, die mit `random.randint(1, 6)` würfelt und die Zahl zurückgibt. Würfle 20-mal und gib True aus, wenn **alle** Würfe zwischen 1 und 6 liegen.",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "True"
        }
      },
      {
        "instruction": "Schritt 2 – Vererbung (Woche 11): Der Gegner `Gegner` ist vorgegeben. Schreibe `Boss(Gegner)`, die mit `super().__init__(...)` **doppelt so viele** HP bekommt. Erzeuge `Boss(\"Drache\", 10, 3)` und gib genau HP: 20 aus.",
        "codeTemplate": "welt = {\n    \"eingang\": {\n        \"beschreibung\": \"Du stehst am Eingang der Drachenhöhle. Es riecht nach Rauch.\",\n        \"ausgaenge\": {\"norden\": \"halle\"},\n    },\n    \"halle\": {\n        \"beschreibung\": \"Eine riesige Halle. Fackeln flackern an den Wänden.\",\n        \"ausgaenge\": {\"sueden\": \"eingang\", \"osten\": \"schatzkammer\", \"westen\": \"quelle\"},\n    },\n    \"quelle\": {\"beschreibung\": \"Eine stille Quelle. Das Wasser funkelt magisch.\", \"ausgaenge\": {\"osten\": \"halle\"}},\n    \"schatzkammer\": {\"beschreibung\": \"Gold, so weit du blicken kannst – und mittendrin schläft der Drache!\", \"ausgaenge\": {\"westen\": \"halle\"}},\n}\n\ndef beschreibe(room_name):\n    raum = welt[room_name]\n    print(f\"📍 {room_name.capitalize()}: {raum['beschreibung']}\")\n    print(\"   Ausgänge:\", \", \".join(raum[\"ausgaenge\"]))\n\ndef gehe(position, richtung):\n    ausgaenge = welt[position][\"ausgaenge\"]\n    if richtung in ausgaenge:\n        neue_position = ausgaenge[richtung]\n        beschreibe(neue_position)\n        return neue_position\n    print(\"🚫 Dort geht es nicht weiter!\")\n    return position\n\nclass Gegenstand:\n    def __init__(self, name, beschreibung):\n        self.name = name\n        self.beschreibung = beschreibung\n\nwelt[\"eingang\"][\"gegenstaende\"] = [Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")]\nwelt[\"halle\"][\"gegenstaende\"] = []\nwelt[\"quelle\"][\"gegenstaende\"] = [Gegenstand(\"Schwert\", \"Ein scharfes Schwert, das neben der Quelle im Stein steckt.\")]\nwelt[\"schatzkammer\"][\"gegenstaende\"] = [Gegenstand(\"Schatz\", \"Der legendäre Schatz von Pyralia!\")]\n\nclass Spieler:\n    def __init__(self, name, position):\n        self.name = name\n        self.position = position\n        self.hp = 20\n        self.inventar = []\n\n    def gehe(self, richtung):\n        ausgaenge = welt[self.position][\"ausgaenge\"]\n        if richtung in ausgaenge:\n            self.position = ausgaenge[richtung]\n            beschreibe(self.position)\n            self.umschauen()\n        else:\n            print(\"🚫 Dort geht es nicht weiter!\")\n\n    def umschauen(self):\n        gegenstaende = welt[self.position][\"gegenstaende\"]\n        if gegenstaende:\n            namen = [g.name for g in gegenstaende]\n            print(\"👀 Du siehst:\", \", \".join(namen))\n        else:\n            print(\"👀 Hier liegt nichts.\")\n\n    def nimm(self, item_name):\n        raum = welt[self.position]\n        for gegenstand in raum[\"gegenstaende\"]:\n            if gegenstand.name == item_name:\n                raum[\"gegenstaende\"].remove(gegenstand)\n                self.inventar.append(gegenstand)\n                print(f\"🎒 Du nimmst: {gegenstand.name} – {gegenstand.beschreibung}\")\n                return\n        print(f\"❓ Hier gibt es kein '{item_name}'.\")\n\n    def zeige_inventar(self):\n        if self.inventar:\n            namen = [g.name for g in self.inventar]\n            print(\"🎒 Im Beutel:\", \", \".join(namen))\n        else:\n            print(\"🎒 Dein Beutel ist leer.\")\n\nimport random\n\nclass Gegner:\n    def __init__(self, name, hp, staerke):\n        self.name = name\n        self.hp = hp\n        self.staerke = staerke\n\n    def ist_besiegt(self):\n        return self.hp <= 0\n\nwelt[\"eingang\"][\"gegner\"] = None\nwelt[\"halle\"][\"gegner\"] = None\nwelt[\"quelle\"][\"gegner\"] = None\nwelt[\"schatzkammer\"][\"gegner\"] = Gegner(\"Drache\", 15, 6)\n\ndef hat_gegenstand(spieler, name):\n    for gegenstand in spieler.inventar:\n        if gegenstand.name == name:\n            return True\n    return False\n\ndef kampf(spieler, gegner):\n    print(f\"⚔️ Kampf: {spieler.name} gegen {gegner.name}!\")\n    bonus = 3 if hat_gegenstand(spieler, \"Schwert\") else 0\n    while spieler.hp > 0 and not gegner.ist_besiegt():\n        schaden = random.randint(1, 6) + bonus\n        gegner.hp -= schaden\n        print(f\"   Du triffst für {schaden} Schaden. ({gegner.name}: {max(gegner.hp, 0)} HP)\")\n        if gegner.ist_besiegt():\n            break\n        gegenschlag = random.randint(1, gegner.staerke)\n        spieler.hp -= gegenschlag\n        print(f\"   {gegner.name} trifft dich für {gegenschlag}. ({spieler.name}: {max(spieler.hp, 0)} HP)\")\n    return spieler.hp > 0\n\ndef pruefe_gegner(spieler):\n    gegner = welt[spieler.position][\"gegner\"]\n    if gegner is not None and not gegner.ist_besiegt():\n        if kampf(spieler, gegner):\n            print(f\"🎉 {gegner.name} wurde besiegt!\")\n\n# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "HP: 20"
        }
      },
      {
        "instruction": "Schritt 3 – Magic Method (Woche 11): Schreibe die Klasse `Gegenstand` mit `__init__(self, name, beschreibung)` und `__str__`, das `Name – Beschreibung` zurückgibt. Gib `Gegenstand(\"Fackel\", \"Sie leuchtet in dunklen Ecken.\")` direkt mit `print` aus.",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Fackel – Sie leuchtet in dunklen Ecken."
        }
      },
      {
        "instruction": "Schritt 4 – Dateien (Woche 8/9): Schreibe `lade_stand(filename)`, die den JSON-Inhalt einer Datei zurückgibt – fehlt sie, gibt sie ein leeres Dictionary `{}` zurück (`try/except FileNotFoundError`). Gib genau Anzahl: 0 für `lade_stand(\"gibtsnicht.json\")` aus (Länge des Ergebnisses).",
        "codeTemplate": "# 🎯 Deine Lösung hier:\n",
        "validation": {
          "type": "output_contains",
          "expected": "Anzahl: 0"
        }
      }
    ]
  }
]
