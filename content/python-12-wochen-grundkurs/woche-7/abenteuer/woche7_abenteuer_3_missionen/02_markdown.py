"""### ⭐⭐☆☆☆ Mission 1: Das Wächter-Siegel

Das erste Tor ist mit drei Wächter-Siegeln verschlossen. Jedes Siegel öffnet sich nur, wenn du den passenden Wächter beschwörst (importierst) und seine Kraft beweist.

**Schritt 1 – Die drei Wächter beschwören:**
Importiere die Module `random`, `time` und `string` (nutze bei mindestens einem einen Alias mit `as`) und gib von jedem eine Kostprobe aus (z.B. `random.randint(1, 6)`, `len(string.ascii_letters)`, `time.time()`)

**Schritt 2 – Das Warte-Ritual des Zeit-Wächters:**
Erzeuge mit `random.uniform(1, 3)` eine zufällige Wartezeit, nutze `time.sleep()` damit und gib vor und nach der Wartezeit eine passende Meldung aus (z.B. „Das Siegel beginnt zu leuchten …“)

**Schritt 3 – Das Runen-Fragment des Wort-Wächters:**
Ziehe mit `random.choice()` einen zufälligen Buchstaben aus `string.ascii_letters` und gib ihn als „Runen-Fragment\" aus

**Schritt 4 – Das Siegel öffnen:**
Gib eine Abschlussmeldung aus, die alle drei Beweise zusammenfasst (z.B. „Alle drei Wächter sind besiegt – das Siegel öffnet sich!“)

**Bonus:** Füge mit `from datetime import datetime` eine Zeitstempel-Meldung hinzu, wann du das Siegel geöffnet hast."""
