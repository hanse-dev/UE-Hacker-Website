"""### Stage 5: Enemy and Fight – Task 1

Write the class `Enemy` with `__init__(self, name, hp, strength)` and the method `is_defeated(self)` (True if `hp <= 0`). Create `Enemy(\"Robot\", 10, 3)`, print `is_defeated()`, set `hp = 0` and print it once more."""