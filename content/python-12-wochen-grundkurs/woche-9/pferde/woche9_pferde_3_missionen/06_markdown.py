"""### ⭐⭐⭐⭐☆ Mission 3: Die CSV-Datenbank

Erstelle ein komplettes CSV-Verwaltungssystem!

**Schritt 1 – CSV-Export:**
Schreibe eine Liste von Pferde-Dictionaries (`name`, `rasse`, `alter`) mit `csv.DictWriter` in `pferde.csv` (mit `newline=\"\"`) und gib eine Bestätigung aus

**Schritt 2 – CSV-Import:**
Lies die Datei mit `csv.DictReader` ein, sammle die Zeilen in einer Liste und gib die Anzahl sowie die erste Zeile aus

**Schritt 3 – Suchfunktion:**
Schreibe eine Funktion, die nur die Zeilen mit einer bestimmten Rasse zurückgibt, und rufe sie auf und gib das Ergebnis aus

**Schritt 4 – Kurzer Bericht:**
Berechne eine kleine Statistik (z.B. Durchschnittsalter, Anzahl pro Rasse) aus den gelesenen Daten und gib sie aus

**Bonus:** Prüfe, ob alle erwarteten Spalten in der CSV-Datei vorhanden sind."""
