"""## System Protocol 5: Nested Conditions

**What it is:** An `if` condition can contain another `if` condition inside it – this is called nesting.

**Important:** Each extra nesting level needs one more level of indentation!"""
