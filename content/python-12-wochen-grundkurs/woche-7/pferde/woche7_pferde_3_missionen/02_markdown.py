"""### ⭐⭐☆☆☆ Mission 1: Der Werkzeug-Importer

In der Werkstatt vom Sonnental-Hof lernst du, wie du Werkzeugkisten (Module) lädst und mit Aliasen arbeitest.

**Schritt 1 – Werkzeugkisten importieren:**
Lade die Module `math`, `random` und `time` mit passenden Aliasen (z.B. `import math as m`) und gib jeweils eine Konstante oder Funktion aus jedem Modul aus (z.B. `math.pi`, `random.randint(1, 10)`)

**Schritt 2 – Wartezeit-Simulation:**
Erstelle eine kleine Funktion oder ein Skript, das eine zufällige Sekundenzahl (1–3) mit `random` erzeugt und `time.sleep()` nutzt und gib vor und nach der Wartezeit eine kurze Meldung aus (z.B. „Warte auf Pferd …“)

**Schritt 3 – Sprung-Berechnung:**
Nutze `math.sin` und `math.pi`, um für einen Winkel (z.B. 90° in Bogenmaß) eine Sprunghöhe oder einen Wert zu berechnen und gib das Ergebnis formatiert aus

**Schritt 4 – Reiter-Generator:**
Erstelle eine Namensliste mit 3–5 Reiter-Namen und wähle mit `random.choice()` einen zufälligen Namen aus und gib den ausgewählten Namen aus

**Bonus:** Füge eine weitere Funktion mit dem `datetime`-Modul hinzu (z.B. aktuelle Uhrzeit anzeigen)."""
