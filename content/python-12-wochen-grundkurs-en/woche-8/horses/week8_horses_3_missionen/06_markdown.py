"""### ⭐⭐⭐⭐☆ Mission 3: The Rider's Guild

You build a small rider's guild using a list of dictionaries and search functions.

**Step 1 – List of rider dictionaries:**
Create a list `riders` where each element is a dictionary with e.g. `name`, `level`, `age`, `experience` (years); create at least 3 riders and print the list

**Step 2 – Add members:**
Add a new rider member with `riders.append({...})` and print the updated list and the number of members with `len(riders)`

**Step 3 – Search function by level:**
Write a function that returns all riders with a given level (e.g. a list of dictionaries); call the function for a level (e.g. 2) and print the found riders

**Step 4 – Statistics and assignment:**
Calculate the average age of the riders (sum of ages / count) and print it; optionally assign a new key-value pair to one or more riders (e.g. `\"horse\": \"Luna\"`) and print the updated list


> 💡 **Tip:** You decide how to implement this – there is no single right way!

**Bonus:** Add keys like `tournaments` (list) or `achievements` to each rider and print an overview."""
