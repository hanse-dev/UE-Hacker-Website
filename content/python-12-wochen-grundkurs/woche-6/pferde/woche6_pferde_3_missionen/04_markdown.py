"""### ⭐⭐⭐☆☆ Mission 2: Der Trainings-Manager

Du verwaltest die Trainingsübungen mit Listen und kleinen Hilfsfunktionen.

**Schritt 1 – Übungsliste anlegen:**
Erstelle eine Liste `uebungen` mit verschiedenen Übungen (z.B. \"Dressur\", \"Springen\", \"Galopp\") und gib die Liste und `len(uebungen)` aus

**Schritt 2 – Übungen hinzufügen:**
Nutze `append()` und `insert()`, um Übungen am Ende und an einer Position (z.B. 0) einzufügen und gib die aktualisierte Liste aus

**Schritt 3 – Übungen entfernen und suchen:**
Entferne mit `remove()` und `pop()`, finde mit `index()` eine Position und prüfe mit `in` und gib die Liste und die gefundene Position aus

**Schritt 4 – Statistik ausgeben:**
Gib Anzahl der Übungen, die aktuelle Liste und optional eine kurze Statuszeile aus

**Bonus:** Speichere zu jeder Übung eine Dauer (z.B. in einer zweiten Liste) und gib eine sortierte Übersicht aus."""
